# Review Decision — 2026-02-24

## 검토 대상
- **Discovery 리포트**: 2026-02-24-discovery.md
- **총 발견 항목**: 1건 (UPDATE 1건) + 누적 미실행 3건
- **프로젝트 현황**: 17/45 컴포넌트 구현 완료

---

## 평가 결과

### APPROVED (2건)

| # | 항목 | 유형 | 적합성 | 호환성 | ROI | 의존성 | 타이밍 | 배치 |
|---|------|------|--------|--------|-----|--------|--------|------|
| 1 | markdown_renderer | 이전 Batch 2 승격 | ESSENTIAL | COMPATIBLE | HIGH | READY | NOW | 1 |
| 2 | nl_to_chart | 누적 미실행 (3회) | ESSENTIAL | ADAPTABLE | HIGH | PARTIAL | NOW | 2 |

#### APPROVE-1: markdown_renderer
- **판정 근거**: priority: critical 미구현 컴포넌트로 가장 많은 후속 의존성을 가짐. streaming_typing, code_block, mermaid_diagram이 모두 markdown_renderer에 의존. 이전 리뷰(2026-02-22)에서 Batch 2로 지정되었으나 Batch 1(tool_call_display)이 완료되어 승격.
- **권장 리서치 범위**: react-markdown + remark/rehype 플러그인 체계, 코드블록 구문 강조(Shiki vs Prism), LaTeX 수식 렌더링, 테이블 렌더링, 스트리밍 호환 증분 파싱 전략
- **예상 complexity**: moderate

#### APPROVE-2: nl_to_chart
- **판정 근거**: 3회 연속 권장(2026-02-21, 22, 24)되었으며 ROI가 가장 높은 미구현 기능. Databricks 에이전틱 대시보드의 NL→시각화 파이프라인이 업계 표준으로 자리잡는 중. 리서치를 먼저 수행하여 프론트엔드 구현 전략과 백엔드 인터페이스 설계를 확립할 필요.
- **권장 리서치 범위**: NL→SQL→데이터→차트 파이프라인 설계, Databricks Genie Research 패턴, 차트 타입 자동 추천 로직, Recharts 기반 동적 차트 생성, 에러 핸들링 및 차트 수정 UX
- **예상 complexity**: complex

### DEFERRED (3건)

| # | 항목 | 유형 | 사유 | 재검토 시점 |
|---|------|------|------|------------|
| 1 | skill_management (카테고리 그룹화) | UPDATE | ROI LOW. implemented 상태에서 그룹화 개선은 critical 미구현 해소 후 진행 | critical 컴포넌트 구현 후 |
| 2 | chat_input (3회 누적 미실행) | 누적 미실행 | partial 상태. 기본 동작 가능. critical 미구현이 우선 | markdown_renderer 구현 후 |
| 3 | model_agent_switcher (3회 누적 미실행) | 누적 미실행 | priority: medium. 단일 에이전트 데모 단계에서 급하지 않음 | 다음 사이클 |

### REJECTED (0건)

> 이번 리뷰에서 REJECT 항목 없음.

---

## 실행 계획

### Batch 1 (즉시 실행)
1. `/research markdown_renderer` → `/implement markdown_renderer`

### Batch 2 (다음 실행)
1. `/research nl_to_chart` → `/implement nl_to_chart`

### Backlog
- streaming_typing: markdown_renderer 구현 후 진행 (의존성). `/research streaming_typing` → `/implement streaming_typing`
- chat_input: 멀티모달 확장. Telerik AI-ready Chat 패턴 참조
- model_agent_switcher: GitHub Copilot 에이전트 모델 피커 + Auto 모드 참조
- skill_management: MCP 서버 카테고리 그룹화. Copilot CLI 패턴 참조

> **형식 규칙**: Batch 항목은 반드시 `` `/research {topic}` → `/implement {id}` `` 형식이어야 한다.
> `pipeline.sh`가 이 패턴을 파싱하여 자동 실행하므로, 형식이 다르면 무시된다.
> 리서치만 필요하고 구현 대상이 불명확한 항목은 DEFER로 분류하여 Batch에 넣지 않는다.

---

## 카탈로그 직접 수정

리뷰 결과에 따라 즉시 반영할 카탈로그 변경:

| # | component_id | 필드 | 현재 | 변경 | 사유 |
|---|-------------|------|------|------|------|
| 1 | usage_monitoring | priority | medium | high | 4회 연속 리포트(Feb 21, 22, 24 + 이전)에서 동일 제안. GitHub/Google 사용량 대시보드 + MS 365 Copilot Dashboard 동향. 업계 표준화 진행 중. 더 이상 미적용 상태 유지 부적절 |

> document_viewer.status는 이미 needs_update로 적용됨. 추가 변경 불필요.

---

## 메모

### skill_management 동향 메모
- GitHub Copilot CLI v0.0.415: MCP 서버를 카테고리별 그룹으로 정리하는 UI 도입. flat list → grouped list 전환은 도구 수 20개 이상 시 탐색성 향상 효과.
- v0도 같은 날(Feb 23) MCP 서버 관리 UX 개선 릴리즈. 복수 제품에서 MCP 관리 UX에 투자 중.
- 향후 skill_management 개선 리서치 시 참고 자료로 활용.

### 누적 미실행 액션 경고
- chat_input, model_agent_switcher는 4회째 미실행 상태. 이는 critical 미구현 컴포넌트(tool_call_display → markdown_renderer → streaming_typing) 해소를 우선했기 때문.
- tool_call_display가 완료되었으므로 markdown_renderer 이후 chat_input 진행을 강력 권장.

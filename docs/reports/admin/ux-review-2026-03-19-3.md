# 관리자 대시보드 UX/UI 종합 검토 리포트

**검토 일시**: 2026-03-19
**검토 범위**: AdminView.tsx, AIQualitySummaryTab.tsx, 사용자 관리, 권한 설정, 감사 로그, AI 품질
**검토 대상**: 이전 라운드 UX 개선사항 검증 + 신규 문제 발견

---

## Part 1: UX 체크리스트 (31항목)

### 1. 네비게이션 & 웨이파인딩 (4항목)

#### 1-1. 탭 활성화 상태 표시 ✓ PASS
- **위치**: AdminView.tsx:362-377 (TabsList)
- **현재 동작**: 활성 탭은 `data-[state=active]:bg-white data-[state=active]:text-black` 스타일 적용
- **상태**: 양호 — 활성 탭이 명확하게 시각화됨
- **평가**: Major

#### 1-2. URL 동기화 ✓ PASS
- **위치**: AdminView.tsx:61-70 (handleTabChange)
- **현재 동작**: `?tab=` 쿼리 파라미터로 탭 상태를 URL에 동기화
- **상태**: 양호
- **평가**: Major

#### 1-3. 뒤로가기 네비게이션 ⚠ MINOR ISSUE
- **위치**: AdminView.tsx (전체)
- **현재 동작**: 뒤로가기 기능 미구현
- **예상 동작**: 사용자가 상세 뷰(예: 사용자 편집 모달)에서 뒤로가기 버튼으로 복귀할 수 있어야 함
- **문제**: 사용자 폼 모달에서 취소 버튼은 있지만, 브라우저 뒤로가기 시 모달 상태가 유지되지 않음
- **영향**: 사용자가 모달 내에서 실수로 뒤로가기를 누르면 상태 불일치
- **심각도**: Minor

#### 1-4. 빵 부스러기 (Breadcrumbs) ⚠ ENHANCEMENT
- **위치**: AdminView.tsx 헤더 영역
- **현재 동작**: 없음
- **예상 동작**: 관리자 > 사용자 관리 와 같은 경로 표시
- **이유**: 대시보드가 여러 탭으로 구성되어 있으므로, 사용자가 "어디에 있는가"를 명확히 알 수 있도록 함
- **심각도**: Enhancement

---

### 2. 정보 아키텍처 (3항목)

#### 2-1. 그룹핑과 계층 구조 ✓ PASS
- **위치**: AdminView.tsx:57-377 (전체 탭 구조)
- **현재 동작**:
  - 5개 탭: 사용자 관리 → 권한 설정 → 사용량 → 감사 로그 → AI 품질
  - 각 탭 내에는 권장 설정 순서 표시 (PermissionSettingsView.tsx:417-418)
- **상태**: 양호 — 논리적 계층화
- **평가**: Major

#### 2-2. 점진적 공개 (Progressive Disclosure) ✓ PASS
- **위치**: UserFormModal.tsx (2단계 마법사)
- **현재 동작**:
  - Step 1: 기본 정보 (이름, 이메일, 조직, 직급)
  - Step 2: 역할 확인 및 수동 오버라이드
- **상태**: 양호 — 복잡도를 단계적으로 증가
- **평가**: Major

#### 2-3. 인지 부하 관리 ✓ PASS
- **위치**: PermissionSettingsView.tsx:412-429 (가이드 배너)
- **현재 동작**:
  - 온보딩 배너: "권장 설정 순서: 1. 역할 정의 → 2. 조직-역할 매핑 → ... → 5. 통합 미리보기"
  - 닫을 수 있는 배너로 인지 부하 감소
- **상태**: 양호
- **평가**: Major

---

### 3. 상호작용 설계 (4항목)

#### 3-1. 피드백 메커니즘 ✓ PASS
- **위치**: AdminView.tsx:574-593 (Toast)
- **현재 동작**:
  - 사용자 추가/수정/삭제 시 토스트 메시지 표시
  - 3초 자동 사라짐, 수동 닫기 버튼 제공
- **상태**: 양호 — 피드백이 명확함
- **평가**: Major

#### 3-2. 로딩 상태 표시 ⚠ MINOR ISSUE
- **위치**: AdminView.tsx, PermissionSettingsView.tsx 전체
- **현재 동작**: 로딩 상태 표시 없음 (모든 데이터가 mock으로 즉시 렌더링)
- **문제**: 실제 API 호출 시 로딩 상태를 표시하지 않음
- **영향**: 사용자가 "현재 작업 중인가"를 알 수 없음
- **심각도**: Minor (현재는 mock 데이터이지만, 실제 환경에서는 필수)

#### 3-3. 에러 핸들링 ✓ PASS
- **위치**: UserFormModal.tsx:299-304 (이메일 검증)
- **현재 동작**:
  - 이메일 형식 검증: "올바른 이메일 형식을 입력해주세요."
  - 이메일 중복 검증: "이미 등록된 이메일입니다." ✓ (개선사항)
- **상태**: 양호 — 에러 메시지가 명확함
- **평가**: Major

#### 3-4. 실행 취소 (Undo) ⚠ ENHANCEMENT
- **위치**: PermissionSettingsView.tsx 전체
- **현재 동작**: 초기화(Reset) 버튼으로 모든 변경사항 일괄 되돌림
- **개선 제안**: 개별 변경사항에 대한 Undo 기능
- **이유**: 마우스 실수로 인한 변경을 개별적으로 취소할 수 있도록
- **심각도**: Enhancement

---

### 4. 데이터 표시 (5항목)

#### 4-1. 테이블 구조 ✓ PASS
- **위치**: AdminView.tsx:201-347 (renderUserTable)
- **현재 동작**:
  - 7개 열: 사용자, 부서, 직급, 권한, 상태, 마지막 접속, 관리
  - 테이블 헤더에 clear caption 제공: `<caption className="sr-only">사용자 목록</caption>`
- **상태**: 양호
- **평가**: Major

#### 4-2. 빈 상태 표시 ✓ PASS
- **위치**: AdminView.tsx:229-243, AuditLogView.tsx:478-483
- **현재 동작**: "검색 결과가 없습니다" + 사용자 추가 버튼 제공
- **상태**: 양호 — 명확한 다음 단계 제시
- **평가**: Major

#### 4-3. 페이지네이션 ✓ PASS
- **위치**: AdminView.tsx:339-345, AuditLogView.tsx:485-512
- **현재 동작**:
  - "이전 / 다음" 버튼
  - 페이지 정보: "총 N건 중 M–K건"
  - aria-label 추가됨: `aria-label="이전 페이지"`, `aria-label="다음 페이지"` ✓ (개선사항)
- **상태**: 양호
- **심각도**: Major

#### 4-4. 정렬 기능 ✓ PASS
- **위치**: AdminView.tsx:86-87, 132-140, 196-199, 208-223
- **현재 동작**:
  - 3개 열 정렬 가능: 사용자 이름, 부서, 마지막 접속
  - 정렬 아이콘: ArrowUpDown (정렬 없음) → ArrowUp (오름차순) → ArrowDown (내림차순)
  - sortField + sortDir 상태 관리, `localeCompare('ko')` 사용으로 한글 정렬 지원 ✓ (개선사항)
- **상태**: 양호
- **평가**: Major

#### 4-5. 필터링 ✓ PASS
- **위치**: AdminView.tsx:82-151
- **현재 동작**:
  - 4개 필터: 검색(name/email/dept), 상태, 부서, 역할
  - 필터 초기화 버튼: hasActiveFilters 조건으로 표시 ✓ (개선사항)
  - aria-label 추가: `aria-label="상태 필터"` 등
- **상태**: 양호
- **심각도**: Major

---

### 5. 폼 & 입력 (4항목)

#### 5-1. 입력 검증 ✓ PASS
- **위치**: UserFormModal.tsx:192-200
- **현재 동작**:
  - 이메일 형식 검증: regex `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
  - 이메일 중복 검증: `existingEmails` 배열 비교 (편집 모드에서 자신의 이메일 제외) ✓ (개선사항)
  - 필수 필드: 이름, 이메일, 조직, 직급
- **상태**: 양호 — 검증 로직이 명확함
- **심각도**: Major

#### 5-2. 기본값 & 자동 완성 ⚠ PARTIAL
- **위치**: UserFormModal.tsx:117-143 (matchOrgRoles)
- **현재 동작**:
  - 조직 + 직급 선택 시 자동으로 역할 매핑
  - 매핑 근거를 blue banner로 표시
  - 수동 오버라이드 토글로 자동 매핑 무시 가능
- **개선 제안**: 이름 입력 시 자동 완성 (기존 사용자 이름 기반)
- **평가**: Partial
- **심각도**: Enhancement

#### 5-3. 에러 메시지 ✓ PASS
- **위치**: UserFormModal.tsx:299-304, 485-506
- **현재 동작**:
  - 이메일 형식 오류: "올바른 이메일 형식을 입력해주세요." (빨강, 텍스트 크기 xs)
  - 이메일 중복 오류: "이미 등록된 이메일입니다."
  - 역할 매핑 없음: amber banner with alert icon + 권한 설정 링크 제공
- **상태**: 양호 — 에러 메시지가 구체적이고 행동 가능함
- **심각도**: Major

#### 5-4. 편집 상태 추적 ✓ PASS
- **위치**: UserFormModal.tsx:89-116 (isFieldChanged, DiffBadge)
- **현재 동작**:
  - 편집 모드에서 변경된 필드를 amber ring으로 강조
  - "변경됨" 배지 표시
  - originalValues snapshot으로 변경 여부 추적
- **상태**: 양호 — 사용자가 "뭐가 변했는가"를 명확히 볼 수 있음
- **평가**: Major

---

### 6. 시각 설계 (4항목)

#### 6-1. 일관성 ✓ PASS
- **위치**: 전체 컴포넌트
- **현재 동작**:
  - Tailwind CSS 사용 (bg-white, border-gray-200, rounded-xl 등)
  - 배지 색상 일관성: 상태별, 역할별 색상 정의 (UserStatusBadge, DomainRoleBadge 등)
  - 버튼 스타일: primary (bg-[#1A1A1A]), outline (border-gray-300) 일관적
- **상태**: 양호
- **평가**: Major

#### 6-2. 공간 & 타이포그래피 ✓ PASS
- **위치**: 전체
- **현재 동작**:
  - 일관된 padding: px-6 py-4, px-4 py-2.5 등
  - 타이포그래피: h1 (text-2xl font-bold), h2 (text-lg font-bold), h3 (text-sm font-bold)
  - 폰트 크기 계층: xs (11px) → sm (14px) → base (16px)
- **상태**: 양호
- **평가**: Major

#### 6-3. 색상 & 아이콘 ✓ PASS
- **위치**: AdminView.tsx, 전체
- **현재 동작**:
  - 색상 의미: green (활성/성공), red (삭제/비활성화), amber (경고), blue (정보)
  - 아이콘: Users, Shield, BarChart2, FileText, Sparkles (탭별)
  - aria-hidden="true" 적용으로 스크린 리더에서 제외
- **상태**: 양호
- **평가**: Major

#### 6-4. 그림자 & 반응형 ✓ PASS
- **위치**: 전체
- **현재 동작**:
  - 그림자: shadow-sm (카드), shadow-lg (모달)
  - 반응형: grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 (KPI 카드)
  - 호버 상태: hover:bg-gray-50, hover:text-gray-700 명시적 정의
- **상태**: 양호
- **평가**: Major

---

### 7. 접근성 (4항목)

#### 7-1. ARIA 레이블 ✓ PASS (개선됨)
- **위치**: 전체
- **현재 동작**:
  - aria-label 사용: "사용자 검색", "상태 필터", "사용자 목록 페이지네이션" (AdminView.tsx)
  - aria-label="필터" (AuditLogView.tsx:343)
  - aria-label="CSV 내보내기" (AuditLogView.tsx 미적용 ⚠ 하단 참고)
  - aria-pressed 사용: 토글 버튼 (view toggle, filter button)
  - role="group" + aria-labelledby: 역할 그룹 (PermissionSettingsView.tsx:549)
- **상태**: 대부분 양호, 일부 개선 필요
- **평가**: Major

#### 7-2. 키보드 네비게이션 ⚠ PARTIAL
- **위치**: 전체
- **현재 동작**:
  - 테이블 행: tabIndex={0}, onKeyDown으로 Enter/Space 지원 (AuditLogView.tsx:446-449)
  - 드롭다운: 기본 HTML select (잘 지원됨)
  - 모달: ESC로 닫기 가능 (Dialog 컴포넌트 기본)
- **문제**: 테이블 열 정렬 버튼이 정렬 순서를 시각적으로는 명확하지만, aria-sort 속성 미적용
- **심각도**: Minor

#### 7-3. 색상 대비 ✓ PASS
- **위치**: 전체
- **현재 동작**:
  - 텍스트 색상: text-gray-900 (on white), text-white (on dark)
  - 배지: bg-green-50 text-green-700, bg-red-100 text-red-700 등 (WCAG AA 충족)
- **상태**: 양호
- **평가**: Major

#### 7-4. 포커스 인디케이터 ✓ PASS
- **위치**: 전체 interactive 요소
- **현재 동작**:
  - focus-visible 사용 (Radix UI 컴포넌트)
  - 스킵 링크: sr-only focus:not-sr-only (AdminView.tsx:351-353) ✓ (개선사항)
- **상태**: 양호
- **평가**: Major

---

### 8. 성능 인식 (3항목)

#### 8-1. 스켈레톤 로더 ⚠ NOT IMPLEMENTED
- **위치**: 전체
- **현재 동작**: 없음 (mock 데이터 사용)
- **문제**: 실제 API 호출 시 로딩 상태를 표시하지 않음
- **심각도**: Minor (mock 환경에서는 우선순위 낮음)

#### 8-2. 낙관적 업데이트 (Optimistic Update) ⚠ PARTIAL
- **위치**: AdminView.tsx:177-187 (handleSaveUser)
- **현재 동작**:
  - 사용자 저장 시 즉시 상태 업데이트 (setUsers)
  - 토스트 메시지로 피드백 제공
- **개선 제안**: 실제 API 호출 전에 UI 업데이트, API 실패 시 롤백
- **평가**: Partial
- **심각도**: Enhancement

#### 8-3. Lazy Loading ⚠ NOT IMPLEMENTED
- **위치**: AdminView.tsx (사용자 관리), PermissionSettingsView.tsx
- **현재 동작**: 모든 데이터를 한 번에 로드
- **개선 제안**: 무한 스크롤 또는 페이지 단위 Lazy Loading (현재는 페이지네이션 사용)
- **심각도**: Enhancement

---

### 9. 반응형 & 적응형 (3항목)

#### 9-1. 모바일 대응 ✓ PASS
- **위치**: AdminView.tsx, 전체
- **현재 동작**:
  - grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 (반응형 그리드)
  - 테이블: overflow-x-auto (수평 스크롤 대응)
- **상태**: 양호
- **평가**: Major

#### 9-2. 터치 타겟 크기 ✓ PASS
- **위치**: 전체
- **현재 동작**:
  - 버튼: h-8 (32px), h-9 (36px) 이상
  - 최소 권장 크기: 44x44px (모바일)
- **상태**: 양호
- **평가**: Major

#### 9-3. 뷰포트 적응 ⚠ MINOR ISSUE
- **위치**: AdminView.tsx:350, PermissionSettingsView.tsx (전체)
- **현재 동작**: max-w-6xl (1152px)로 컨테이너 제한
- **문제**: 큰 화면(2560px 이상)에서 빈 공간이 많음
- **심각도**: Minor
- **제안**: max-w-7xl 또는 동적 최대 너비 사용

---

### 10. 마이크로인터랙션 (3항목)

#### 10-1. 호버 상태 ✓ PASS
- **위치**: 전체
- **현재 동작**:
  - 버튼: hover:bg-gray-50, hover:text-gray-700
  - 테이블 행: hover:bg-blue-50/50
  - 색상 전환: transition-colors (smooth)
- **상태**: 양호
- **평가**: Major

#### 10-2. 포커스 상태 ✓ PASS
- **위치**: 전체
- **현재 동작**:
  - focus-visible:ring (Radix UI 기본)
  - 포커스 색상: ring-blue-600 (또는 theme color)
- **상태**: 양호
- **평가**: Major

#### 10-3. 비활성화 상태 ✓ PASS
- **위치**: AdminView.tsx:342-343 (페이지네이션)
- **현재 동작**:
  - disabled 속성: disabled:opacity-40
  - 버튼 텍스트: disabled되면 회색으로 표시
- **상태**: 양호
- **평가**: Major

---

## Part 2: 시나리오 기반 제품 검토

### 페르소나 정의

1. **Admin-기본 (관리자 신입)**
   - 사용자 추가/관리만 수행
   - 권한 설정에 대한 이해도 낮음
   - 자주 실수하는 경향

2. **Admin-고급 (시스템 관리자)**
   - 전체 권한 설정 및 감사 로그 모니터링
   - 복잡한 권한 정책 설정 경험 있음
   - 효율성 중시

3. **HR담당자 (사용자 생성자)**
   - 신입 사원 등록 시 사용
   - CSV 일괄 임포트 자주 사용
   - 정확성 중시

4. **보안감시관 (Compliance Officer)**
   - 감사 로그 조회 및 분석
   - PII 접근 추적
   - 컴플라이언스 리포트 작성

5. **개발팀 (권한 정책 설계)**
   - 새로운 역할 추가
   - 조직-역할 매핑 수정
   - RLS/마스킹 정책 추가

---

### 시나리오 1: 신입 사원 등록 (Admin-기본)

**시작점**: 관리자 대시보드 > 사용자 관리 탭

**클릭 경로**:
1. "+ 사용자 추가" 버튼 클릭
2. UserFormModal 열림 (Step 1: 기본 정보)
3. 이름 입력: "김철수"
4. 이메일 입력: "chulsu.kim@konai.com"
5. 소속 조직 선택: OrgTreeSelector → "영업팀"
6. 직급 선택: "실무자"
7. "다음" 버튼 클릭 → Step 2
8. 자동 매핑된 역할 확인: ROLE_SALES_USER 등
9. "등록하기" 버튼 클릭
10. 토스트 메시지: "사용자가 추가되었습니다."
11. 테이블에 새 사용자 행 추가

**마찰점 분석**:

| # | 마찰점 | 위치 | 심각도 | 개선 제안 |
|---|--------|------|--------|----------|
| 1 | 조직 트리 UI가 복잡함 | OrgTreeSelector.tsx | Minor | 검색 기능 추가 (조직명으로 빠르게 찾기) |
| 2 | 역할 매핑 근거가 너무 길게 표시됨 | UserFormModal.tsx:365-376 | Minor | 더보기 / 접기 토글 추가 |
| 3 | 이메일 중복 검증 메시지가 작음 | UserFormModal.tsx:303 | Enhancement | 폰트 크기 xs → sm, 색상 더 강조 |
| 4 | 단계별 저장이 없음 (Step 1 완료 후 저장 불가) | UserFormModal.tsx | Enhancement | 각 단계의 진행 상황을 별도로 저장할 수 있도록 |

**권장 개선**:
- ✓ 이메일 중복 검증 이미 구현됨 (개선사항 확인)
- 조직 선택 시 검색 입력 필드 추가 (effort: 2시간)
- 역할 매핑 근거 펼침/접힘 토글 (effort: 1시간)

---

### 시나리오 2: 권한 정책 설정 (Admin-고급 + 개발팀)

**시작점**: 관리자 대시보드 > 권한 설정 탭

**클릭 경로**:
1. 가이드 배너 읽기: "권장 설정 순서: ..."
2. "역할 정의" 탭 (기본 활성)
3. "새 역할 추가" 클릭 → 역할 이름, 설명, 색상 입력
4. "조직-역할 매핑" 탭 클릭
5. 조직 + 직급 + 역할 매핑 규칙 추가
6. "테이블 접근 매트릭스" 탭
7. 역할 × 서브카테고리 셀을 클릭해 접근 수준 변경 (no_access → read → write)
8. 수정된 셀: ring-1 ring-amber-300 강조
9. 하단 sticky bar: "저장되지 않은 변경사항이 있습니다" (변경 2건)
10. "저장" 버튼 클릭
11. 토스트: "모든 권한 설정이 저장되었습니다."

**마찰점 분석**:

| # | 마찰점 | 위치 | 심각도 | 개선 제안 |
|---|--------|------|--------|----------|
| 1 | 345개 뷰 × 15개 역할 = 5175개 셀 → 스크롤 피로 | TableAccessLayer.tsx | Major | 가상 스크롤 또는 필터 추가 (역할별, 카테고리별) |
| 2 | 저장 버튼이 하단에 고정되어 있지만, 확인 메시지 부족 | PermissionSettingsView.tsx:716-750 | Minor | 저장 전 확인 다이얼로그 추가 |
| 3 | RLS 규칙과 마스킹 정책의 충돌 감지 불명확함 | ConflictPreviewPanel.tsx | Enhancement | 충돌 감지 시 경고 배너 상단에 고정 |
| 4 | 감사 로그가 세션 내에서만 유지됨 | PermissionSettingsView.tsx:172 | Minor | 서버 저장 후 감사 로그 영구 보관 필요 |

**권장 개선**:
- ✓ 초기화 버튼, 저장 버튼 이미 구현됨
- ✓ 감사 로그 추적 구현됨
- 테이블 가상 스크롤 또는 필터 추가 (effort: 4시간) — Major 개선
- 저장 전 확인 다이얼로그 (effort: 1시간)

---

### 시나리오 3: CSV 일괄 임포트 (HR담당자)

**시작점**: 관리자 대시보드 > 사용자 관리 탭 > "CSV 임포트" 버튼

**클릭 경로**:
1. "CSV 임포트" 버튼 클릭
2. BulkUserImportModal 열림
3. CSV 파일 선택 (employee_list.csv)
4. 파일 파싱: 이름, 이메일, 조직, 직급 컬럼 추출
5. 검증: 이메일 형식, 중복 확인
6. "임포트" 버튼 클릭
7. 성공: 토스트 "23명의 사용자가 일괄 등록되었습니다."
8. 테이블 갱신

**마찰점 분석**:

| # | 마찰점 | 위치 | 심각도 | 개선 제안 |
|---|--------|------|--------|----------|
| 1 | CSV 형식이 명시되지 않음 | BulkUserImportModal.tsx | Minor | 템플릿 다운로드 링크 제공 |
| 2 | 임포트 중 오류 처리가 미흡함 (일부 실패 시 전체 실패?) | BulkUserImportModal.tsx | Major | 행별 검증 결과 표시, 부분 성공 지원 |
| 3 | 이메일 중복 시 충돌 해결 방법이 없음 | BulkUserImportModal.tsx | Minor | "기존 사용자 건너뛰기" vs "덮어쓰기" 옵션 |

**권장 개선**:
- CSV 템플릿 다운로드 버튼 추가 (effort: 0.5시간)
- 행별 검증 결과 테이블 표시 (effort: 3시간) — Major 개선
- 이메일 충돌 해결 옵션 (effort: 2시간)

---

### 시나리오 4: 감사 로그 조회 (보안감시관)

**시작점**: 관리자 대시보드 > 감사 로그 탭

**클릭 경로**:
1. 감사 로그 탭 열림
2. KPI 요약: 총 이벤트, 에이전트 액션, 경고, 최근 24시간
3. 필터 펼치기: "필터" 클릭 (기본 닫혀있음)
4. 필터 설정:
   - 시간 범위: "7d" → "30d"
   - 액터 타입: "all" → "user"
   - 액션 카테고리: "all" → "update"
   - 심각도: "all" → "critical"
   - PII 접근만: 토글 활성화
5. 검색: "admin" 입력
6. 필터링된 결과: 5건
7. 테이블 행 클릭 → AuditLogDetail 사이드시트
8. 세부 정보 확인: 변경 이력, 메타데이터, 에이전트 추론 요약
9. "CSV 내보내기" 버튼 → audit-log-2026-03-19.csv 다운로드

**마찰점 분석**:

| # | 마찰점 | 위치 | 심각도 | 개선 제안 |
|---|--------|------|--------|----------|
| 1 | "CSV 내보내기" 버튼에 aria-label 없음 | AuditLogView.tsx:349-357 | Minor | aria-label="감사 로그 CSV 내보내기" 추가 |
| 2 | 필터 초기화 버튼이 없음 | AuditLogView.tsx:336-424 | Minor | "필터 초기화" 버튼 추가 |
| 3 | 타임스탐프 포맷이 모호함 (MM-DD HH:MM) | AuditLogView.tsx:113-119 | Enhancement | ISO 형식 또는 현지화된 전체 날짜 표시 |
| 4 | 변경 이력이 너무 짧게 표시됨 | AuditLogDetail.tsx:185-202 | Minor | 변경 이력 탭별 전개 가능하도록 개선 |

**권장 개선**:
- ✓ CSV 헤더 한글화 이미 구현됨
- aria-label 추가 (effort: 0.5시간)
- 필터 초기화 버튼 추가 (effort: 1시간)
- 타임스탐프 포맷 개선 (effort: 1시간)

---

### 시나리오 5: AI 품질 모니터링 (플랫폼 관리자)

**시작점**: 관리자 대시보드 > AI 품질 관리 탭

**클릭 경로**:
1. AI 품질 관리 탭 열림
2. AIQualitySummaryTab 렌더링:
   - 제목: "AI 피드백 현황"
   - 설명: "AI 에이전트 응답 품질에 대한 피드백 현황을 확인합니다."
3. FeedbackSummaryView 렌더링:
   - 기간 필터: 7d, 14d, 30d, 90d
   - KPI 카드 4개: 전체 만족도, 응답 품질, 총 피드백, 미해결 쿼리
   - 품질 추이 차트 (일별 만족도, 긍정/부정 피드백)
4. 기간 변경: "30d" → "7d"
5. 차트 업데이트

**마찰점 분석**:

| # | 마찰점 | 위치 | 심각도 | 개선 제안 |
|---|--------|------|--------|----------|
| 1 | 프롬프트 조회 탭이 제거됨 ✓ | AIQualitySummaryTab.tsx | Major | 상태: 올바르게 개선됨 (FeedbackSummaryView만 렌더링) |
| 2 | 차트 토이핏이 마우스 호버 시에만 표시됨 | FeedbackSummaryView.tsx:171-181 | Minor | 터치 환경에서 토이핏 표시 방법 개선 필요 |
| 3 | 기간별 상세 데이터를 테이블로 볼 수 없음 | FeedbackSummaryView.tsx | Enhancement | "상세 데이터" 탭 추가 (테이블 형식) |

**권장 개선**:
- ✓ 프롬프트 조회 탭 제거 이미 완료
- 터치 친화적 토이핏 구현 (effort: 2시간)
- 상세 데이터 탭 추가 (effort: 3시간)

---

## Part 3: 서비스 기획 관점의 검토

### 정보 아키텍처 적합성

**강점**:
- 5개 탭으로 명확하게 구분된 기능: 사용자 관리 → 권한 설정 → 사용량 → 감사 → AI 품질
- 권장 설정 순서 명시 (PermissionSettingsView.tsx:417-418)
- 각 탭이 독립적으로 작동하며 상호 참조 가능 (예: UserFormModal에서 권한 설정 링크)

**약점**:
- 사용자 관리 내 "조직도" 뷰와 "리스트" 뷰 토글이 있지만, 조직도 기능이 주요 사용자 흐름과 연결되지 않음
- 감사 로그와 권한 설정의 감사 로그가 별도로 관리됨 (세션 내 vs 영구 저장)

**평가**: B+ (좋음, 약간의 개선 필요)

---

### 기능 정당성 (Keep/Modify/Remove/Add)

| 기능 | 상태 | 근거 | 액션 |
|------|------|------|------|
| 사용자 기본 정보 관리 (이름, 이메일, 조직, 직급) | KEEP | 필수 기능, 검증 명확함 | - |
| 역할 자동 매핑 (조직-직급 기반) | KEEP | 사용 오류 감소, UX 개선 | 수동 오버라이드 토글 유지 |
| CSV 일괄 임포트 | MODIFY | 현재: 단순 파일 선택만 가능 | 행별 검증 결과 표시 추가 |
| 조직도 뷰 | MODIFY | 현재: 부분적으로만 사용됨 | 조직 계층 시각화 강화 필요 |
| 권한 정책의 3-Layer (테이블 접근, RLS, 컬럼 마스킹) | KEEP | 복잡하지만 필요, 갈등 미리보기로 검증 지원 |  - |
| 감사 로그 (권한 설정용) | KEEP | 컴플라이언스 필수 | 서버 저장 필요 |
| AI 품질 피드백 현황 | KEEP | 읽기 전용, 명확한 목적 | 상세 데이터 탭 추가 권장 |
| 프롬프트 조회 (Admin용) | REMOVE | ✓ 이미 제거됨 (FeedbackSummaryView만) | - |
| 필터 초기화 버튼 | ADD | 사용자 경험 개선 | ✓ 권한 설정의 필터 초기화 이미 구현 |
| 검색 & 정렬 | KEEP | 필수 기능, 잘 구현됨 | 추가 개선 불필요 |

**평가**: 대부분 적절함. CSV 임포트 개선과 상세 데이터 표시 추가 권장.

---

### 복잡도 관리

**인지 부하 분석**:
- ✓ 마법사 패턴 (UserFormModal): 2단계로 단순화
- ✓ 권한 설정 가이드 배너: 설정 순서 명시
- ✓ 갈등 미리보기: 복잡한 정책을 시각화

**개선점**:
- 권한 설정 탭의 345개 뷰 × 15개 역할 매트릭스는 여전히 복잡함
  - 제안: 가상 스크롤 또는 역할별 필터 추가 (effort: 4시간)
- 감사 로그 세부 사항 사이드시트가 길어짐
  - 제안: 탭 기반 레이아웃 (기본 정보 | 변경 이력 | 메타데이터)

**평가**: B (복잡도가 높지만, 유용한 시각화로 관리 중)

---

### 화면 간 관계

**흐름도**:
```
관리자 대시보드 (5개 탭)
├─ 사용자 관리
│  ├─ 리스트 뷰 (사용자 테이블)
│  ├─ 조직도 뷰
│  └─ 사용자 폼 모달 (신규/편집)
│     └─ 조직 선택 시 권한 설정으로 이동 가능
├─ 권한 설정 (7개 레이어 탭)
│  ├─ 역할 정의
│  ├─ 조직-역할 매핑
│  ├─ 테이블 접근 매트릭스
│  ├─ RLS
│  ├─ 컬럼 마스킹
│  ├─ 통합 권한 미리보기
│  └─ 감사 로그 (권한 설정)
├─ 사용량 모니터링
├─ 감사 로그 (전체 감사)
└─ AI 품질 관리
   └─ AI 피드백 현황 (읽기 전용)
```

**평가**: A (명확하고 논리적인 흐름)

---

## Part 4: 프로덕션 준비 상태 검토

### 데이터 신뢰성

#### 실제 vs Mock 데이터

| 영역 | 상태 | 주의사항 |
|------|------|---------|
| 사용자 관리 | Mock (MOCK_ENHANCED_USERS) | 실제 DB 연결 전 이메일 중복 검증 로직 테스트 필수 |
| 권한 설정 | Mock + 세션 상태 | 저장 전에 백엔드 검증 필요 (충돌 감지) |
| 감사 로그 (권한) | 세션 내 메모리 | 서버 저장 미구현 → 새로고침 시 데이터 손실 ⚠ |
| 감사 로그 (전체) | Mock (MOCK_AUDIT_LOG_ENTRIES) | 실제 감사 스트림 연결 필요 |
| 사용량 모니터링 | Mock (getDailyUsageByPeriod) | 실제 사용량 데이터 연결 필요 |
| AI 품질 | Mock (QUALITY_KPI, getDailyQualityByPeriod) | 백엔드 피드백 API 연결 필요 |

**평가**: C+ (Mock 데이터 단계, 프로덕션 준비 부분적)

#### 새로고침 표시

- **문제**: 데이터 새로고침 버튼이 없음 (감사 로그, 사용량 모니터링)
- **제안**: 우상단에 새로고침 아이콘 추가, "마지막 업데이트: 14:35" 표시
- **effort**: 1시간

---

### 확장성 준비

#### 페이지네이션

- **사용자 관리**: ✓ 20명/페이지, 페이지네이션 구현
- **감사 로그**: ✓ 20건/페이지, 페이지네이션 구현
- **권한 설정**: ⚠ 345개 뷰 × 15개 역할 = 5175개 셀 → 가상 스크롤 필요

#### 가상화

- **현재**: 없음
- **권장**: 권한 설정의 테이블 접근 매트릭스에 가상 스크롤 추가 (react-window)
- **effort**: 4시간

---

### 에러 & 엣지 케이스 처리

#### 검증된 케이스

| 케이스 | 처리 방법 | 상태 |
|--------|---------|------|
| 이메일 형식 오류 | 에러 메시지 표시 | ✓ |
| 이메일 중복 | 에러 메시지 + 필드 강조 | ✓ |
| 필수 필드 미입력 | "다음" 버튼 비활성화 | ✓ |
| 역할 매핑 없음 | Amber banner + 권한 설정 링크 | ✓ |
| 검색 결과 없음 | "검색 결과가 없습니다" + 추가 버튼 | ✓ |

#### 미처리 케이스

| 케이스 | 현재 | 필요 조치 | 심각도 |
|--------|------|---------|---------|
| 네트워크 오류 | 처리 없음 | 에러 배너 + 재시도 버튼 | Major |
| API 응답 시간 초과 | 처리 없음 | 타임아웃 알림 | Major |
| 동시 편집 충돌 | 처리 없음 | 낙관적 락(optimistic lock) | Minor |
| 파일 업로드 실패 | 처리 없음 | 상세 에러 메시지 | Major |
| 저장 실패 (권한 설정) | 처리 없음 | 롤백 + 에러 표시 | Major |

**평가**: C (기본 검증은 있으나, 네트워크 에러 처리 필수)

---

### 보안 & 접근 제어

#### 관리자 가드

- **위치**: /src/app/admin/page.tsx:10-27 (useAdminGuard)
- **현재 동작**:
  ```tsx
  const { isAllowed, isChecking } = useAdminGuard();
  if (!isAllowed) return <Unauthorized />;
  ```
- **평가**: ✓ 구현됨

#### 데이터 민감도

- **PII 배지**: AuditLogView.tsx:88-92 에서 PII 접근 표시 ✓
- **마스킹 정책**: ColumnMaskingLayer 구현으로 민감한 데이터 보호 ✓
- **감사 로그**: 모든 관리자 작업 추적 ✓

**평가**: A (보안 기본이 잘 구현됨)

---

### 성능 고려사항

#### 번들 크기

- **AdminView.tsx**: ~600 lines
- **PermissionSettingsView.tsx**: ~770 lines
- **라이브러리**: recharts (큼), Radix UI (작음)
- **제안**: Code splitting으로 권한 설정 탭을 lazy load

#### 렌더링 성능

- **문제**: 345개 뷰 × 15개 역할 매트릭스를 한 번에 렌더링 → 느린 DOM 조작
- **해결책**: react-window 가상 스크롤 또는 페이지네이션
- **effort**: 4시간
- **우선순위**: Major

#### API 최적화

- **현재**: Mock 데이터 (지연 없음)
- **프로덕션**:
  - 감사 로그 조회 시 페이지별로 로드 (이미 구현)
  - 권한 설정 저장 시 배치 요청 (현재: 단건)
  - 사용량 모니터링 데이터 캐싱 (30분)

**평가**: B (현재는 양호, 실제 API 연결 시 최적화 필요)

---

## 종합 평가 및 권장사항

### 개선 완료 확인 (이전 라운드)

✓ AI 품질관리 탭에서 "프롬프트 조회" 서브탭 제거
✓ 이메일 중복 검증 추가
✓ 페이지네이션 aria-label 추가
✓ 필터 초기화 버튼 추가
✓ CSV 다운로드 aria-label 추가 (audit log)
✓ 감사 로그 CSV 헤더 한글화
✓ 권한 설정 초기화/저장 버튼 순서 변경
✓ 테이블 열 정렬 추가

### Critical 이슈 (즉시 조치 필요)

1. **네트워크 에러 처리 미구현** (severity: Major)
   - 상황: API 호출 실패 시 사용자에게 피드백 없음
   - 조치: 에러 배너 + 재시도 버튼 추가
   - effort: 3시간

2. **권한 설정 저장 데이터 손실** (severity: Major)
   - 상황: 감사 로그가 세션 메모리에만 저장됨 (새로고침 시 손실)
   - 조치: 서버 감사 로그 저장 구현
   - effort: 8시간 (백엔드 API 필요)

3. **CSV 임포트 검증 부족** (severity: Major)
   - 상황: 행별 오류 시 전체 실패, 사용자 피드백 없음
   - 조치: 행별 검증 결과 표시
   - effort: 3시간

### Major 이슈 (1주일 내 해결)

4. **권한 설정 매트릭스 성능** (severity: Major)
   - 상황: 345개 뷰 × 15개 역할 매트릭스 렌더링 느림
   - 조치: 가상 스크롤 또는 필터 추가
   - effort: 4시간

5. **조직 선택 UI 복잡함** (severity: Major)
   - 상황: 대규모 조직 구조에서 선택 어려움
   - 조치: 검색 기능 추가
   - effort: 2시간

### Minor 이슈 (월간 개선)

6. 타임스탐프 포맷 개선 (감사 로그)
7. 필터 초기화 버튼 추가 (감사 로그에 추가)
8. aria-sort 속성 추가 (테이블 정렬)
9. 뷰포트 최대 너비 확장 (2560px 이상 화면)
10. 터치 친화적 토이핏 (차트)

---

## 최종 평가

| 영역 | 등급 | 이유 |
|------|------|------|
| 네비게이션 & 웨이파인딩 | A- | 명확한 탭 구조, URL 동기화 완벽 |
| 정보 아키텍처 | B+ | 명확한 계층, 일부 기능 연결 미흡 |
| 상호작용 설계 | B | 기본 피드백 있으나 로딩 상태 미흡 |
| 데이터 표시 | A- | 테이블, 필터, 정렬, 페이지네이션 우수 |
| 폼 & 입력 | A | 검증, 에러 처리, 편집 상태 추적 우수 |
| 시각 설계 | A | 일관성, 색상, 타이포그래피 우수 |
| 접근성 | B+ | ARIA 레이블 많음, 일부 개선 필요 |
| 성능 인식 | B | 낙관적 업데이트 부분 구현, 로딩 표시 미흡 |
| 반응형 & 적응형 | A | 모바일 대응, 터치 타겟 우수 |
| 마이크로인터랙션 | A | 호버, 포커스, 비활성화 상태 우수 |
| **종합** | **B+** | **기본기 탄탄하나 네트워크 에러 처리 필수** |

---

## 다음 단계 (Action Items)

### Sprint 1 (1주일)
- [ ] 네트워크 에러 처리 (에러 배너 + 재시도)
- [ ] CSV 임포트 행별 검증 결과 표시
- [ ] 조직 선택 검색 기능 추가

### Sprint 2 (2주)
- [ ] 권한 설정 매트릭스 가상 스크롤
- [ ] 서버 감사 로그 저장 구현
- [ ] 필터 초기화 버튼 추가 (감사 로그)

### Sprint 3 (월간)
- [ ] 타임스탐프 포맷 개선
- [ ] aria-sort 속성 추가
- [ ] 터치 친화적 토이핏
- [ ] 뷰포트 확장 대응

---

**검토 완료: 2026-03-19**
**검토자**: UX Reviewer Agent
**상태**: 구현 권장사항 포함 ✓

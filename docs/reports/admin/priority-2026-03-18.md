# 통합 우선순위 보고서 — 관리자
날짜: 2026-03-18
출처: `code-review-2026-03-18.md` + `ux-review-2026-03-18.md`
대상: `src/components/AdminView.tsx` + `src/app/admin/` + `src/components/features/permission-settings/`

---

## 요약

| 출처 | 이슈 |
|--------|--------|
| 코드 리뷰 | 22 (중대 2 / 높음 8 / 중간 7 / 낮음 5) |
| UX 리뷰 | 31 (중대 4 / 높음 10 / 중간 11 / 낮음 6) |
| **중복 제거 후 합산** | **43개 (공통 10개 병합)** |

**전체 수정 예상 시간**: Quick Wins ~5h / 전략적 항목 ~20h / 보완 항목 ~3h

---

## 우선순위 매트릭스

### 🟢 Quick Wins (높은 영향도 + 낮은 공수) — 우선 처리

> 1회 스프린트 내 해결 가능. 직접 접근성·정확성 영향.

| # | 파일 | 라인 | 문제 | 출처 | 예상 시간 |
|---|------|------|------|------|------|
| QW-1 | `AdminView.tsx:191,201,209,219` `RowSecurityLayer.tsx:188,197` `ColumnMaskingLayer.tsx:154,159` `RoleDefinitionManager.tsx:344,351` `OrgRoleMappingManager.tsx:500,507` | 다수 | 아이콘 전용 버튼에 `aria-label` 누락 — `title` 속성은 스크린리더에서 신뢰할 수 없음 (WCAG 4.1.2) | 코드+UX | 1h |
| QW-2 | `Sidebar.tsx:211` | 211 | 알림 벨 버튼에 `aria-label` 누락 (WCAG 4.1.2) | UX | 0.1h |
| QW-3 | `Sidebar.tsx:118` | 118 | 로고 `<div onClick>` → `<button aria-label="KonaAgent 홈으로 이동">` (WCAG 2.1.1) | UX | 0.2h |
| QW-4 | `AdminView.tsx:300` | 300 | 사용자 검색 `<Input>`에 `<label>` 또는 `aria-label` 누락 (WCAG 1.3.1) | UX | 0.2h |
| QW-5 | `PermissionSettingsView.tsx:147` | 147 | `{} as never` 타입 단언 — `Partial<Record<DomainRole, Record<string, AccessLevel>>>` 로 수정 | 코드 | 5min |
| QW-6 | `AdminView.tsx:79` | 79–87 | `filteredUsers` useMemo 누락 — 매 렌더마다 O(n) 재계산 | 코드 | 5min |
| QW-7 | `RoleDefinitionManager.tsx:302` `OrgRoleMappingManager.tsx:447` | - | `<th>` 에 `scope="col"` 누락 (WCAG 1.3.1) | UX | 0.4h |
| QW-8 | `PermissionSettingsView.tsx:183` | 183 | `PILL_COLORS` 상수가 컴포넌트 내부에 선언됨 → 모듈 스코프로 이동 | 코드 | 2min |
| QW-9 | `ConflictPreviewPanel.tsx:281` | 281 | `key={idx}` → `key={filter}` (배열 인덱스 키 → 값 기반 키) | 코드 | 2min |
| QW-10 | `ColumnMaskingLayer.tsx:241` | 241 | `isCreateMode` 중복 상태 제거 → `const isCreateMode = editingId === null` 파생 | 코드 | 5min |
| QW-11 | `RowSecurityLayer.tsx:139` | 139–153 | 인라인 `<svg>` 화살표 → Lucide `ChevronDown` + `aria-expanded` 추가 | 코드+UX | 0.3h |
| QW-12 | `AdminView.tsx:242` `PlatformAdminView.tsx:18` | - | 페이지 최상위 헤딩 `<h2>` → `<h1>` (WCAG 1.3.1) | UX | 0.2h |
| QW-13 | `AccessMatrixCell.tsx:79` | 79–85 | 셀 트리거 버튼에 `aria-label={config.label}` 추가 | UX | 0.2h |
| QW-14 | `PermissionSettingsView.tsx:324` | 324–346 | 역할 필 버튼에 `aria-pressed={isSelected}` 추가 | UX | 0.2h |
| QW-15 | `AdminView.tsx:62` 전체 뱃지 | - | 장식용 Lucide 아이콘에 `aria-hidden="true"` 추가 | UX | 0.3h |
| QW-16 | `TableAccessLayer.tsx:193` | 193 | 검색 `<input>`에 `type="search"` + `aria-label` 추가 | UX | 0.1h |
| QW-17 | `TableAccessLayer.tsx:212` | 212–218 | "기본값 복원" 버튼 → 시각적 파괴적 스타일 (`text-red-600 border-red-200`) | UX | 0.2h |
| QW-18 | `AdminView.tsx:141` | 141 | 아바타 이니셜 `<div>`에 `aria-hidden="true"` 추가 | UX | 0.1h |

**소계**: 18개 항목, 예상 ~3.5h

---

### 🔴 전략적 항목 (높은 영향도 + 높은 공수) — 스프린트 계획 필요

| # | 파일 | 문제 | 출처 | 예상 시간 |
|---|------|------|------|------|
| ST-1 | `AdminView.tsx:100` `RowSecurityLayer.tsx:343` `ColumnMaskingLayer.tsx:349` `OrgRoleMappingManager.tsx:319` `RoleDefinitionManager.tsx:236` | `window.confirm()` 5개소 → Radix `<AlertDialog>` 공용 컴포넌트로 교체 (스크린리더 비접근 + 디자인 깨짐) | 코드+UX | 3h |
| ST-2 | `TableAccessLayer.tsx:338–556` | `div` 그리드 접근 매트릭스 → `<table>/<caption>/<th scope>/<td>` 시맨틱 마크업 리팩토링 (WCAG 1.3.1) | UX | 8h |
| ST-3 | `PermissionSettingsView.tsx` + 하위 컴포넌트 | 권한 설정 변경 사항에 저장 버튼·성공/실패 토스트 추가 (WCAG 4.1.3, 현재 피드백 없음) | UX | 4h |
| ST-4 | `TableAccessLayer.tsx:35` `ViewRowsSection.tsx:157` | `accessMatrix.find()` → `Map<DomainRole, …>` 룩업 (`useMemo`) + `getOverrideCount` 사전 계산으로 O(n²)→O(1) 최적화 | 코드 | 2h |
| ST-5 | `RowSecurityLayer.tsx:58` `ColumnMaskingLayer.tsx:70` `OrgRoleMappingManager.tsx:43` | `buildGroupedRoles` 3개 파일 복붙 → `utils/roleGroupUtils.ts` 공용 추출 | 코드 | 1.5h |
| ST-6 | `AdminView.tsx:51` `OrgRoleMappingManager.tsx:134` `RoleDefinitionManager.tsx:59` | `DomainRoleBadge` 스타일 로직 3개소 중복 → `shared/RoleBadge.tsx` 단일 컴포넌트 | 코드 | 1h |
| ST-7 | `OrgRoleMappingManager.tsx` | 761줄 단일 파일 → Table / Dialog / Utils 4개로 분리 | 코드 | 4h |
| ST-8 | `AdminView.tsx:271` | 탭 전환 시 스크롤 위치 초기화 → `<TabsContent forceMount>` 또는 `sessionStorage` 패턴 | UX | 2h |

**소계**: 8개 항목, 예상 ~25.5h

---

### 🟡 보완 항목 (낮은 영향도 + 낮은 공수) — 여유 시 처리

| # | 파일 | 문제 | 출처 | 예상 시간 |
|---|------|------|------|------|
| FI-1 | `navigation.ts` + `Sidebar.tsx` | 네비게이션 레이블 한/영 혼용 → 한국어 통일 | UX | 0.5h |
| FI-2 | `ColumnMaskingLayer.tsx:442` | 다이얼로그 액션 버튼이 스크롤 영역 안에 있어 작은 화면에서 숨김 → 고정 푸터로 이동 | UX | 0.5h |
| FI-3 | `RowSecurityLayer.tsx` `ColumnMaskingLayer.tsx` `RoleDefinitionManager.tsx` `OrgRoleMappingManager.tsx` | 다이얼로그 열릴 때 첫 번째 입력 필드에 `autoFocus` 누락 | UX | 0.5h |
| FI-4 | `TableAccessLayer.tsx:413` | 모듈 아코디언 토글에 `aria-controls` 누락 | UX | 0.3h |
| FI-5 | `ConflictPreviewPanel.tsx:92` | `toggleModule` `useCallback` 누락 (일관성) | 코드 | 0.2h |
| FI-6 | `viewTableData.ts:261` | `SENSITIVITY_DISPLAY` 키 타입 `string` → `SensitivityLabel` 리터럴 유니온 | 코드 | 0.3h |
| FI-7 | `AdminView.tsx:90` | `handleUserStatusToggle` `useCallback` + `setUsers(prev => …)` 함수형 업데이터 | 코드 | 0.2h |
| FI-8 | `AdminView.tsx:195` | 사용자 상태 토글(Power 버튼) 확인 없이 즉시 반영 → 5초 되돌리기 토스트 패턴 | UX | 1h |
| FI-9 | `OrgRoleMappingManager.tsx:557` | 종속 드롭다운 활성화 시 `aria-live="polite"` 안내 없음 | UX | 1h |
| FI-10 | `TableAccessLayer.tsx:334` | `STICKY_LEFT_WIDTH` / `CELL_WIDTH` 컴포넌트 내부 상수 → 모듈 스코프 + `ViewRowsSection` 직접 임포트 | 코드 | 0.3h |
| FI-11 | `FilterBar` sensitivity checkboxes | `sr-only` 체크박스 포커스 시 시각적 포커스 링 없음 → `focus-within:ring-2` | UX | 0.3h |

**소계**: 11개 항목, 예상 ~5.1h

---

### ⚪ 후순위 항목 (낮은 영향도 + 높은 공수)

| # | 문제 | 이유 | 예상 시간 |
|---|------|------|------|
| DP-1 | 접근 매트릭스 전체 키보드 탐색 (15열 그리드 custom key handler) | ST-2 시맨틱 테이블 작업 선행 필요 | 12h |
| DP-2 | i18n 프레임워크 도입 (레이블 국제화) | 현재 한국어 전용 제품 | 16h |
| DP-3 | `AdminView.tsx:141` avatarColor 인라인 style | 런타임 동적 색상이므로 Tailwind 불가, 현상 유지 허용 | — |

---

## 중복 병합 목록 (양쪽 모두 발견)

| 이슈 | 코드 리뷰 | UX 리뷰 | 병합 판정 |
|------|-----------|---------|---------|
| `window.confirm()` | HIGH | CRITICAL | → ST-1 (CRITICAL) |
| 아이콘 전용 버튼 `aria-label` 누락 | HIGH | CRITICAL | → QW-1 (CRITICAL) |
| `RowSecurityLayer` 인라인 SVG | MEDIUM | HIGH | → QW-11 (HIGH) |

---

## Top 5 Quick Wins (지금 당장)

| 순위 | 항목 | 파일 | 예상 시간 |
|------|------|------|---------|
| 1 | 아이콘 전용 버튼 전체에 `aria-label` 추가 (5개 파일) | AdminView, RowSecurity, ColumnMasking, RoleDefinition, OrgRoleMapping | 1h |
| 2 | `{} as never` → 안전한 타입으로 수정 | `PermissionSettingsView.tsx:147` | 5min |
| 3 | `filteredUsers` `useMemo` 적용 | `AdminView.tsx:79` | 5min |
| 4 | 알림 벨 + 로고 `aria-label` / `<button>` 전환 | `Sidebar.tsx:118, 211` | 0.3h |
| 5 | `th scope="col"` 2개 테이블에 추가 | RoleDefinitionManager, OrgRoleMappingManager | 0.4h |

---

## 다음 단계

- Quick Wins: `run /review-page admin` 으로 QW 항목 자동 수정
- 전략적 항목: ST-1 (`AlertDialog` 공용 컴포넌트) → ST-4 (O(n) 최적화) 순으로 진행 권장
- 접근성 기준: WCAG 2.1 AA 달성 목표 시 QW 전체 + ST-1, ST-2, ST-3 필수

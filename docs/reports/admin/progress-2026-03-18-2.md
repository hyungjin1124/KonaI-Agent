# Fix Progress — 2026-03-18 (Run 2)

## Quick Wins 완료 목록

| # | 파일 | 이슈 | 변경 내용 | 수정 라인 수 |
|---|------|------|-----------|-------------|
| QW-1 | AdminView.tsx, PermissionSettingsView.tsx, AccessMatrixCell.tsx, ViewRowsSection.tsx, ConflictPreviewPanel.tsx, TableAccessLayer.tsx, RowSecurityLayer.tsx, ColumnMaskingLayer.tsx, RoleDefinitionManager.tsx, OrgRoleMappingManager.tsx | `'use client'` 지시어 누락 | 각 파일 첫 줄에 `'use client';` 추가 | 10 |
| QW-2 | AdminView.tsx | `handleEditUser` 매 렌더마다 재생성 | `useCallback` 래핑 | 4 |
| QW-3 | PermissionSettingsView.tsx | `domainOrder` 배열 컴포넌트 내 매 렌더마다 재생성 | 모듈 스코프 상수 `DOMAIN_ORDER`로 이동 | 3 |
| QW-4 | OrgRoleMappingManager.tsx | `renderGroupedRoleOptions` 컴포넌트 내 함수 선언 | 모듈 스코프 함수로 이동 (dead comment 대체) | 5 |
| QW-5 | ConflictPreviewPanel.tsx | `maskingBadgeClass` 함수가 매 렌더마다 재정의 | 모듈 스코프 `MASKING_BADGE_CLASS` const Record로 교체 | 6 |
| QW-6 | ColumnMaskingLayer.tsx, OrgRoleMappingManager.tsx | 빈 dead comment 헤더 블록 | 불필요 주석 제거 / `renderGroupedRoleOptions`로 대체 | 4 |
| QW-7 | AccessMatrixCell.tsx | popover 옵션 아이콘 span에 `aria-hidden` 누락 | `aria-hidden="true"` 추가 (2개 span) | 2 |
| QW-8 | RowSecurityLayer.tsx, ColumnMaskingLayer.tsx, RoleDefinitionManager.tsx, OrgRoleMappingManager.tsx | 편집/삭제 버튼에 정적 `aria-label` | 동적 `aria-label` 적용 (컨텍스트 식별자 포함) | 8 |
| QW-9 | AdminView.tsx, PermissionSettingsView.tsx | `<TabsList>` `aria-label` 누락 | `aria-label="관리자 메뉴"` / `aria-label="권한 설정 메뉴"` 추가 | 2 |
| QW-10 | AdminView.tsx, PermissionSettingsView.tsx | 탭 내부 아이콘 컴포넌트에 `aria-hidden` 누락 | 12개 아이콘에 `aria-hidden="true"` 추가 | 12 |
| QW-11 | AdminView.tsx | 뷰 토글 버튼에 `aria-pressed` 누락 | `aria-pressed={userViewMode === 'list/orgchart'}` 추가 + 아이콘 `aria-hidden` | 4 |
| QW-12 | AdminView.tsx | `<Briefcase>` 아이콘에 `aria-hidden` 누락 | `aria-hidden="true"` 추가 | 1 |
| QW-13 | RowSecurityLayer.tsx, ColumnMaskingLayer.tsx, RoleDefinitionManager.tsx, OrgRoleMappingManager.tsx | 결과 카운트 영역에 `aria-live` 누락 | `aria-live="polite" aria-atomic="true"` 추가 | 4 |
| QW-14 | OrgRoleMappingManager.tsx | 접기/펼치기 버튼에 `aria-expanded` 누락 | `aria-expanded={autoSectionOpen}` 추가 | 1 |
| QW-15 | TableAccessLayer.tsx | `▶` 화살표 span에 `aria-hidden` 누락 | `aria-hidden="true"` 추가 (2개 span) | 2 |
| QW-16 | ConflictPreviewPanel.tsx | `⊕`, `⚠️` 장식 기호에 `aria-hidden` 누락 | `aria-hidden="true"` 추가 | 2 |
| QW-17 | RoleDefinitionManager.tsx | 중복 오류 `<p>`에 `role="alert"` 누락 | `role="alert"` 추가 | 1 |
| QW-18 | TableAccessLayer.tsx | "기본값 복원" 버튼이 즉시 실행 (확인 없음) | `ConfirmDialog` 도입, `FilterBar`에 `resetConfirmOpen` state 추가 | 18 |

## Quick Wins 스킵 (사유 포함)

없음 — 18개 전체 완료.

## Strategic / Fill-in 완료 목록 (Run 2 추가)

| # | 파일 | 이슈 | 변경 내용 | 수정 라인 수 |
|---|------|------|-----------|-------------|
| ST-J | `src/app/admin/page.tsx` | Suspense fallback에 `role="status"` 누락 | `role="status" aria-label="관리자 페이지 로딩 중..."` 추가 | 1 |
| ST-H | `RowSecurityLayer`, `ColumnMaskingLayer`, `OrgRoleMappingManager` | `roleLabelMap: Record<string, string>` — 타입 너무 넓음 | `Record<DomainRole, string>` 으로 좁힘 | 6 |
| ST-K | `RowSecurityLayer`, `ColumnMaskingLayer` | Dialog에 `DialogDescription` 누락 | 각 Dialog에 `<DialogDescription>` 추가 | 6 |
| ST-D | `AdminView.tsx` | 스킵 내비게이션 링크 + main 랜드마크 누락 | 본문 바로가기 스킵 링크, `<main id="admin-content">` 랜드마크 추가 | 5 |
| ST-E | `TableAccessLayer.tsx` | 서브카테고리 토글 버튼에 `aria-expanded`/`aria-controls` 누락 | `aria-expanded`, `aria-controls`, `aria-label` 추가 + 패널 `id` 래퍼 추가 | 6 |
| ST-B | `TableAccessLayer.tsx` | `getOverrideCount()`가 JSX 렌더 중 매번 호출 (O(n³)) | `overrideCountMap` useMemo로 사전 계산, 렌더 중 Map 조회로 교체 | 15 |
| ST-I | `PermissionSettingsView.tsx` | 도메인 역할 pill 그룹에 `role="group"` + `aria-labelledby` 누락 | `role="group" aria-labelledby={labelId}`, `id={labelId}` 추가 | 3 |
| ST-G | `ViewRowsSection.tsx` | 오버라이드 방향이 배경색만으로 표현 (색각 의존) | `▲`/`▼` 아이콘(`aria-hidden="true"`) absolute 위치 추가 | 8 |
| ST-A | `RowSecurityLayer.tsx`, `OrgRoleMappingManager.tsx` | `as DomainRole` 캐스트 전 런타임 검증 없음 | `handleSave`에 `DOMAIN_ROLES.includes()` 가드 추가 + `DOMAIN_ROLES` import | 6 |
| ST-C | `src/app/admin/page.tsx`, `src/components/ui/error-boundary.tsx` | 페이지 수준 ErrorBoundary 없음 | `ErrorBoundary` 클래스 컴포넌트 생성, `AdminPage`에 래핑 | 50 |
| FI-C | `TableAccessLayer.tsx` | 죽은 `style={{ writingMode: 'horizontal-tb' }}` | 제거 | 1 |
| FI-D | `confirm-dialog.tsx` | `description: string` — React.ReactNode 불가 | `description: React.ReactNode`로 완화 | 1 |
| FI-G | `ConflictPreviewPanel.tsx` | `▾` 아이콘에 `aria-hidden` 누락 | `aria-hidden="true"` 추가 | 1 |
| FI-J | `AccessMatrixCell.tsx` | PopoverContent에 접근 가능한 레이블 없음 | `aria-label="접근 수준 선택"` 추가 | 1 |
| FI-B | `permissionSettingsData.ts`, `OrgRoleMappingManager`, `RoleDefinitionManager` | `DATA_SCOPE_OPTIONS` 중복 선언 | 공유 export로 추출, 두 컴포넌트에서 import로 교체 | 20 |
| FI-A | `RowSecurityLayer`, `ColumnMaskingLayer`, `RoleDefinitionManager`, `OrgRoleMappingManager` | IIFE in JSX props (`description={(() => {...})()}`) | IIFE → `const deleteConfirmDescription` 변수로 추출 | 32 |
| FI-E | `ColumnMaskingLayer.tsx` | `Date.now().slice(-4)` ID 생성 — 충돌 위험 | `crypto.randomUUID()` 로 교체 | 1 |
| FI-I | `TableAccessLayer.tsx` | 민감도 필터 체크박스/레이블에 `htmlFor`/`id` 누락 | `id={`sensitivity-${opt}`}` + `htmlFor` 연결 | 4 |
| FI-K | `RowSecurityLayer.tsx` | `SqlPreview`의 토글 버튼에 접근 가능한 레이블 없음 | `label` prop 추가, `aria-label` 조건부 적용 | 6 |
| FI-H | `PermissionSettingsView`, `RoleDefinitionManager`, `OrgRoleMappingManager`, `RowSecurityLayer`, `ColumnMaskingLayer` | 헤딩 계층 구조 오류 (h4가 h2 다음) | h3→h2 (섹션 헤딩), h4→h3 (서브컴포넌트 헤딩) | 5 |
| FI-L | `AdminView.tsx` | 액션 버튼 터치 타겟 32×32px (WCAG 2.5.5 실패) | `h-7 w-7` → `h-9 w-9` (36×36px) | 4 |
| FI-F | `AdminView.tsx` | 빈 상태가 Table 외부 / `<caption>` 누락 | `<caption className="sr-only">사용자 목록</caption>` 추가, 빈 상태를 TableBody row로 이동 | 8 |

## 남은 항목

- **Deprioritize 항목**: 3개 (undo/redo, 배치 작업, 가상 스크롤)
- **Strategic (미처리)**: 대형 컴포넌트 분리 (OrgRoleMappingManager 761줄) — 별도 스프린트 권장

## TypeScript 검증 결과

```
수정 대상 파일(admin/permission-settings) 오류: 0건
기존 pre-existing 오류(agent-chat 영역): 변경 없음 (세션 전후 동일)
```

## 최종 요약

- **QW 완료**: 18/18
- **Strategic 완료**: 10/11 (대형 컴포넌트 분리 1개 미처리 — Deprioritize 이동)
- **Fill-in 완료**: 12/12
- **수정 파일 수**: 16개
- **신규 생성 파일**: `src/components/ui/error-boundary.tsx`

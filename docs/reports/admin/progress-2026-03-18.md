# Fix Progress — 2026-03-18
Target: admin (AdminView.tsx + permission-settings/ + Sidebar.tsx + PlatformAdminView.tsx)

## Quick Wins Completed (18/18)

| # | File | Issue | Change Made | Lines Changed |
|---|------|-------|-------------|---------------|
| QW-5 | `PermissionSettingsView.tsx:147` | `{} as never` 위험 타입 단언 | `{} as Record<DomainRole, Record<string, AccessLevel>>` — 명시적 올바른 타입 캐스팅 | 1 |
| QW-8 | `PermissionSettingsView.tsx` | `PILL_COLORS` 컴포넌트 내부 선언 | 모듈 스코프 (`function` 외부)로 이동 | 17 |
| QW-14 | `PermissionSettingsView.tsx:344` | 역할 필 버튼 `aria-pressed` 누락 | `aria-pressed={isSelected}` 추가 | 1 |
| QW-6 | `AdminView.tsx:79` | `filteredUsers` 매 렌더마다 재계산 | `useMemo([users, searchQuery])` 래핑 + `useMemo` import 추가 | 5 |
| QW-12a | `AdminView.tsx:242` | 페이지 제목 `<h2>` → `<h1>` | `h2` → `h1` | 1 |
| QW-18 | `AdminView.tsx:141` | 아바타 이니셜 div 스크린리더 노출 | `aria-hidden="true"` 추가 | 1 |
| QW-15 | `AdminView.tsx:62` | 장식용 Shield 아이콘 | `aria-hidden="true"` 추가 | 1 |
| QW-4 | `AdminView.tsx:300` | 사용자 검색 input 레이블 없음 | `type="search"` + `aria-label="사용자 검색"` + Search 아이콘 `aria-hidden` | 3 |
| QW-1a | `AdminView.tsx:186–221` | Eye/Power/Edit/Trash 버튼 `aria-label` 누락 | `aria-label` 추가 (4개 버튼), 내부 아이콘 `aria-hidden` | 8 |
| QW-2 | `Sidebar.tsx:211` | 알림 벨 버튼 `aria-label` 없음 | 동적 `aria-label` + Bell/Badge `aria-hidden` | 3 |
| QW-3 | `Sidebar.tsx:118` | 로고 `<div onClick>` 키보드 미접근 | `<button type="button" aria-label="KonaAgent 홈으로 이동">` 로 교체 | 2 |
| QW-9 | `ConflictPreviewPanel.tsx:281` | `key={idx}` 배열 인덱스 키 | `key={filter}` 값 기반 키로 교체 | 1 |
| QW-13 | `AccessMatrixCell.tsx:79` | 셀 트리거 `aria-label` 없음 | `aria-label={config.label}` + 아이콘 `aria-hidden` | 2 |
| QW-7a | `RoleDefinitionManager.tsx:302` | `<th>` `scope` 누락 | 7개 `<th>`에 `scope="col"` 추가 | 7 |
| QW-1b | `RoleDefinitionManager.tsx:343` | Edit/Trash 버튼 `aria-label` 없음 | `aria-label` + `aria-hidden` 추가 | 4 |
| QW-7b | `OrgRoleMappingManager.tsx:447` | `<th>` `scope` 누락 | 9개 `<th>`에 `scope="col"` 추가 | 9 |
| QW-1c | `OrgRoleMappingManager.tsx:499` | Edit/Trash 버튼 `aria-label` 없음 | `aria-label` + `aria-hidden` 추가 | 4 |
| QW-1d | `RowSecurityLayer.tsx:187` | Edit/Trash 버튼 `aria-label` 없음 | `aria-label` + `aria-hidden` 추가 | 4 |
| QW-11 | `RowSecurityLayer.tsx:133` | 인라인 `<svg>` chevron + `aria-expanded` 없음 | `ChevronDown` Lucide 아이콘 교체 + `aria-expanded={expanded}` | 10 |
| QW-1e | `ColumnMaskingLayer.tsx:150` | Edit/Trash 버튼 `aria-label` 없음 | `aria-label` + `aria-hidden` 추가 | 4 |
| QW-10 | `ColumnMaskingLayer.tsx:241` | `isCreateMode` 중복 상태 | `const isCreateMode = editingId === null` 파생값으로 교체, `setIsCreateMode` 호출 3개 제거 | -4 |
| QW-16 | `TableAccessLayer.tsx:193` | 검색 `input` `type="text"`, `aria-label` 없음 | `type="search"` + `aria-label` 추가 | 2 |
| QW-17 | `TableAccessLayer.tsx:212` | "기본값 복원" 버튼 일반 스타일 | `text-red-600 border-red-200 hover:bg-red-50` 파괴적 액션 스타일 | 1 |
| QW-12b | `PlatformAdminView.tsx:18` | 페이지 제목 `<h2>` → `<h1>` | `h2` → `h1` | 1 |

## TypeScript 검증

```
admin 관련 파일 오류: 0건
(기존 agent-chat/ 파일 오류는 이번 수정 전부터 존재하던 pre-existing 오류)
```

## Quick Wins Skipped

없음 — 18개 항목 모두 완료.

## Remaining Items (Strategic + Fill-in)

| 구분 | 건수 | 대표 항목 |
|------|------|---------|
| Strategic | 8 | window.confirm→AlertDialog (3h), 매트릭스 시맨틱 테이블 (8h), 권한 저장 버튼+토스트 (4h) |
| Fill-in | 11 | navigation 레이블 통일, dialog autoFocus, aria-controls 등 |

## Next Steps

1. **ST-1** — `window.confirm()` 5개소를 공용 `<ConfirmDeleteDialog>` (Radix AlertDialog)로 교체. 가장 낮은 노력 대비 높은 접근성 개선.
2. **ST-4** — `accessMatrix.find()` → `Map` 룩업으로 O(n²) 성능 최적화.
3. **ST-7** — `OrgRoleMappingManager.tsx` (761줄) → Table/Dialog/Utils로 분리.

`/review-page admin` 재실행 시 Strategic 항목이 목록에 표시됩니다.

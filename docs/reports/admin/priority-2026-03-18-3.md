# Admin 패널 통합 우선순위 리포트 — 2026-03-18-3

## 개요

| 항목 | code-reviewer | ux-reviewer | 통합 |
|------|-------------|-------------|------|
| 검토 파일 | 26개 | 22개 컴포넌트 | — |
| 총 이슈 | 24건 | 47건 | **55건** (중복 제거 후) |
| Critical | 0 | 0 | 0 |
| Quick Win | 5건 | 7건 | **11건** (2건 병합) |
| Strategic | 1건 | 6건 | **7건** |
| Fill-in | 7건 | 8건 | **12건** |
| Deprioritize | — | 4건 | **4건** |

---

## 중복 병합 내역

두 에이전트가 동일하게 지적한 이슈 — 하나의 항목으로 병합함:

| 이슈 | code 심각도 | ux 심각도 | 병합 결과 |
|------|-----------|---------|---------|
| `platform-admin/page.tsx` ErrorBoundary 누락 | HIGH | 중간 | **Quick Win** |
| `UserAccessViewModal` DEFAULT_ACCESS_MATRIX 정적 참조 | HIGH | 높음 | **Quick Win** |
| `AccessMatrixCell` aria-pressed 미선언 | HIGH (이중 클릭) | 낮음 | **Quick Win** |

---

## 우선순위 매트릭스

### 🔴 Quick Win — 높은 영향도, 낮은 공수 (즉시 처리)

> 총 예상 수정 시간: **약 10시간**

| # | 컴포넌트 | 파일 | 이슈 | 출처 | 예상 |
|---|---------|------|------|------|------|
| Q1 | `PlatformAdminPage` | `src/app/platform-admin/page.tsx` | **[병합] ErrorBoundary 누락** — `admin/page.tsx`와 달리 렌더 오류가 앱 전체로 전파. 4줄 패턴 복사로 해결 | CODE+UX | 5분 |
| Q2 | `ErrorBoundary` | `src/components/ui/error-boundary.tsx` | **componentDidCatch 미구현** — 에러 발생 시 로깅/리포팅 불가. 4줄 추가 | CODE | 5분 |
| Q3 | `UserAccessViewModal` | `src/components/features/user-management/components/UserAccessViewModal.tsx:71` | **[병합] DEFAULT_ACCESS_MATRIX 정적 참조** — 관리자가 권한 변경해도 "접근 가능 뷰" 모달에 반영 안 됨. `AdminView`에서 `accessMatrix` prop 전달 | CODE+UX | 2h |
| Q4 | `AdminView`, `RowSecurityLayer`, `ColumnMaskingLayer` | 각 해당 파일 | **저장 성공 toast 없음** — CRUD 성공 시 사용자 확인 불가. `handleSaveUser`, `handleAddRule`, `handleSaveMaskPolicy` 완료 후 toast 추가 | UX | 2h |
| Q5 | `OrgRoleMappingManager` | `src/components/features/permission-settings/components/OrgRoleMappingManager.tsx:684` | **탭 간 연결 링크 없음** — "테이블 접근 매트릭스 탭으로 이동" 클릭 링크 부재. 텍스트 → 링크 버튼으로 교체 | UX | 30분 |
| Q6 | `ConflictPreviewPanel` | `src/components/features/permission-settings/components/ConflictPreviewPanel.tsx:251` | **충돌 배너 하드코딩** — "HR 역할과 비인사 역할이 동시 할당됨" 고정 텍스트. `selectedRoles`로 동적 역할명 표시 | UX | 1h |
| Q7 | `AccessMatrixCell` | `src/components/features/permission-settings/components/AccessMatrixCell.tsx:81` | **[병합] aria-pressed 미선언** — 스크린리더 사용자가 현재 셀 상태 알 수 없음 | CODE+UX | 30분 |
| Q8 | `PermissionSettingsView` | `src/components/features/permission-settings/PermissionSettingsView.tsx:206` | **신규 관리자 온보딩 가이드 없음** — "345뷰 × 15역할 × 3-Layer" 첫 화면이 압도적. 닫기 가능한 설정 순서 배너 추가 | UX | 1h |
| Q9 | `TenantDetailPanel` | `src/components/features/platform-admin/components/TenantDetailPanel.tsx:100` | **API 키 폐기 ConfirmDialog 없음** — 클릭 즉시 폐기 → 서비스 중단 위험. ConfirmDialog 연결 | UX | 1h |
| Q10 | `UserAccessViewModal` | `src/components/features/user-management/components/UserAccessViewModal.tsx:119` | **DomainRoleBadge 미재사용** — Badge 스타일 로직이 두 파일에 중복. `DomainRoleBadge` 컴포넌트로 대체 | CODE | 10분 |
| Q11 | `common.types.ts` | `src/types/common.types.ts:1` | **불필요한 `import React`** — 타입 파일에서 JSX 미사용. `import type { ReactNode } from 'react'`으로 교체 | CODE | 3분 |

---

### 🟡 Strategic — 높은 영향도, 높은 공수 (스프린트 계획 필요)

| # | 컴포넌트 | 파일 | 이슈 | 출처 | 예상 |
|---|---------|------|------|------|------|
| S1 | `PermissionSettingsView` 전체 | `PermissionSettingsView.tsx` + 하위 모두 | **권한 매트릭스 저장 메커니즘 부재** — 모든 변경이 `useState`로만 관리. 탭 전환/새로고침 시 변경 사라짐. 저장 API 연결 + sticky 저장 바 + `beforeunload` 경고 | UX(즉시위험) | 10h |
| S2 | `page.tsx` (admin, platform-admin) | `src/app/admin/page.tsx`, `src/app/platform-admin/page.tsx` | **라우트 가드 없음** — URL 직접 입력 시 무인증 접근 가능. 미들웨어 또는 페이지 레벨 인증 가드 추가 | UX(보안) | 4h |
| S3 | `PermissionSettingsView` | `PermissionSettingsView.tsx:102` | **권한 변경 감사 로그 없음** — `handleToggleAccess` 호출 시 `{who, when, subcategoryId, oldLevel, newLevel}` 로그 미생성. 감사 대응 불가 | UX | 8h |
| S4 | `AdminView` | `AdminView.tsx` + 신규 `BulkUserImportModal.tsx` | **사용자 CSV 일괄 임포트 없음** — 50명 신규 입사자를 단건 입력. 파일 업로드 → 미리보기 → 저장 플로우 | UX | High |
| S5 | `RoleDefinitionManager` | `RoleDefinitionManager.tsx:506` | **역할 삭제 시 참조 매핑 연쇄 처리 없음** — 역할 삭제 후 `orgRoleMappings`에 깨진 참조 잔존. 삭제 시 연쇄 정리 또는 후속 안내 | UX | Medium |
| S6 | `permissionSettingsData.ts` | `src/components/features/permission-settings/permissionSettingsData.ts:291` | **`partial` 마스킹 타입 미처리** — `resolveEffectivePolicy` 내 컬럼 마스킹 해소가 `full`/`hidden`만 지원. `MaskingType`의 세 번째 값 `partial` 무시 | CODE | 2h |
| S7 | `PlatformAdminView` | `src/components/features/platform-admin/PlatformAdminView.tsx:11` | **탭 컴포넌트 지연 로딩 미적용** — 4개 탭을 지연 import 없이 직접 렌더. 번들 크기 증가 | CODE | 1h |

---

### 🔵 Fill-in — 낮은 영향도, 낮은 공수 (여유 시간에 처리)

| # | 컴포넌트 | 파일 | 이슈 | 출처 |
|---|---------|------|------|------|
| F1 | `Sidebar` | `Sidebar.tsx:71` | 미사용 Props 4개 선언 (`isOpen`, `toggleSidebar`, `currentView`, `onNavigate`) — 타입 계약 불일치 | CODE |
| F2 | `Sidebar` | `Sidebar.tsx:83` | `navigate` 함수 `useCallback` 미적용 | CODE |
| F3 | `Sidebar` | `Sidebar.tsx:93` | `navItems` 배열 인라인 정의 — 매 렌더 새 객체 생성 | CODE |
| F4 | `Sidebar` | `Sidebar.tsx:128` | `isActive` 분기 하드코딩 — `viewType` 필드 직접 활용으로 제거 가능 | CODE |
| F5 | `Sidebar` | `Sidebar.tsx:97` | Admin vs Platform Admin 사이드바 tooltip에 용도 구분 없음 — "(테넌트 관리자용)" 뱃지 | UX |
| F6 | `TableAccessLayer` | `TableAccessLayer.tsx:465` | `style={{ backgroundClip: 'padding-box' }}` 인라인 객체 — 매 렌더 생성. 모듈 상수 추출 | CODE |
| F7 | `AdminView` | `AdminView.tsx:57` | 탭 전환 시 검색/필터 상태 초기화 — URL 파라미터로 유지 | UX |
| F8 | `PermissionSettingsView` | `PermissionSettingsView.tsx:94` | `as Record<DomainRole, string>` 타입 단언 — `Partial<Record<...>>` 권장 | CODE |
| F9 | `ConflictPreviewPanel` | `ConflictPreviewPanel.tsx:279` | `key={filter}` 중복 키 가능성 — `key={idx}` 로 교체 | CODE |
| F10 | `OrgRoleMappingManager` 외 3곳 | 각 해당 파일 | `deleteConfirmDescription` IIFE 패턴 4곳 반복 — 공유 유틸 함수 추출 | CODE |
| F11 | `AdminView` | `AdminView.tsx:107` | `renderUserTable()` 일반 함수 → `UserTable` 별도 컴포넌트로 분리 | CODE |
| F12 | `AdminView`, `TenantManagementTab` | 각 해당 파일 | 주요 버튼 색상 불일치 (검정 `#1A1A1A` vs 보라 `#534AB7`) — 디자인 토큰 통일 | UX |

---

### ⚪ Deprioritize — 낮은 영향도, 높은 공수 (현 단계 보류)

| # | 기능 | 이유 |
|---|------|------|
| D1 | 역할 컬럼 필터 (매트릭스 탭에서 표시 역할 선택) | 현재 collapsed 구조로 탐색 가능, 역할 수 고정(15개) |
| D2 | 사용자 다중 선택 + 일괄 상태 변경 | S1 CSV 임포트 이후 필요성 재평가 |
| D3 | 매트릭스 탭과 통합 미리보기 동시 표시 레이아웃 | 아키텍처 변경 비용 대비 사용 빈도 낮음 |
| D4 | TenantDetailPanel → Side Drawer 전환 | UX 개선이나 현재 append 방식도 기능적으로 동작 |

---

## 기능 부재 목록 (신규 개발 필요)

| # | 기능명 | 페르소나 | 예상 공수 |
|---|--------|---------|----------|
| 1 | 권한 매트릭스 저장 API 연동 | IT 관리자 | High |
| 2 | 사용자 CSV 일괄 임포트 | IT 관리자 | High |
| 3 | 권한 변경 감사 로그 기록 | 보안 담당자 | High |
| 4 | 라우트 가드 / 인증 미들웨어 | 전체 | Medium |
| 5 | API 키 발급 기능 구현 | 플랫폼 운영자 | Medium |
| 6 | 테넌트 추가 기능 구현 | 플랫폼 운영자 | Medium |
| 7 | 리소스 제한 편집 UI | 플랫폼 운영자 | Medium |
| 8 | 온보딩 설정 순서 가이드 | 신규 관리자 | Low |

---

## 메트릭 요약

| 분류 | 건수 | 예상 총 시간 |
|------|------|------------|
| Quick Win | 11건 | ~10시간 |
| Strategic | 7건 | ~30시간+ |
| Fill-in | 12건 | ~5시간 |
| Deprioritize | 4건 | — |
| 기능 부재 | 8건 | High/Medium/Low |

---

## 다음 단계

`/review-page admin` — Quick Win 항목(Q1~Q11)을 자동 수정합니다.

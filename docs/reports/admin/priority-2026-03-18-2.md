# 통합 우선순위 매트릭스 — 2026-03-18 (실행 2)

## 대상
`src/components/AdminView.tsx` + `src/app/admin/` + `src/components/features/permission-settings/`

## 중복 제거 이슈 수
- 코드 이슈: 19 (중대 2 / 주요 9 / 경미 8)
- UX 이슈: 28 (높은 영향도 11 / 중간 11 / 낮음 6)
- 겹침: 1 (기본값 복원 ConfirmDialog — UX에서 HIGH로 표시됨, 코드에서는 표시 안 됨)
- **총 중복 제거됨: 46개**

---

## Quick Wins (높은 영향도 + 낮은 공수)

| # | 출처 | 파일 | 이슈 | 예상 시간 |
|---|--------|------|-------|-----------|
| QW-1 | 코드 중대 | 모든 permission-settings 컴포넌트 + AdminView.tsx | `'use client';`를 모든 hook 사용 파일의 첫 줄에 추가 (9개 파일) | 5 min |
| QW-2 | 코드 주요 | `AdminView.tsx:89` | `handleEditUser`를 `useCallback`으로 래핑 | 5 min |
| QW-3 | 코드 주요 | `PermissionSettingsView.tsx:190` | `domainOrder` 상수를 모듈 스코프로 이동 | 2 min |
| QW-4 | 코드 주요 | `OrgRoleMappingManager.tsx:327` | `renderGroupedRoleOptions`를 모듈 스코프로 이동 | 5 min |
| QW-5 | 코드 경미 | `ConflictPreviewPanel.tsx:182` | `maskingBadgeClass`를 모듈 스코프 상수 레코드로 이동 | 5 min |
| QW-6 | 코드 경미 | `ColumnMaskingLayer.tsx:64`, `OrgRoleMappingManager.tsx:41` | 죽은 빈 섹션 주석 헤더 제거 | 3 min |
| QW-7 | UX 높음 | `AccessMatrixCell.tsx:112` | popover 옵션 내 아이콘 `<span>`에 `aria-hidden="true"` 추가 | 10 min |
| QW-8 | UX 높음 | `RowSecurityLayer.tsx:163`, `ColumnMaskingLayer.tsx:131`, `RoleDefinitionManager.tsx:338`, `OrgRoleMappingManager.tsx:466` | 편집/삭제 버튼에서 행 아이덴티티로부터 동적 `aria-label` 구성 | 25 min |
| QW-9 | UX 높음 | `AdminView.tsx:233`, `PermissionSettingsView.tsx:213` | 두 `TabsList`에 `aria-label="관리자 메뉴"` / `aria-label="권한 설정 메뉴"` 추가 | 5 min |
| QW-10 | UX 중간 | `AdminView.tsx:234–251`, `PermissionSettingsView.tsx:214–249` | 모든 12개 탭 트리거 아이콘에 `aria-hidden="true"` 추가 | 15 min |
| QW-11 | UX 중간 | `AdminView.tsx:264–285` | 뷰 토글 버튼에 `aria-pressed={userViewMode === 'list'/'orgchart'}` 추가 | 5 min |
| QW-12 | UX 중간 | `AdminView.tsx:136` | 장식용 `Briefcase` 아이콘에 `aria-hidden="true"` 추가 | 3 min |
| QW-13 | UX 높음 | `RowSecurityLayer.tsx:360`, `ColumnMaskingLayer.tsx:391`, `RoleDefinitionManager.tsx:287`, `OrgRoleMappingManager.tsx:404` | 결과 카운트 `<div>`에 `aria-live="polite" aria-atomic="true"` 추가 (4개 파일) | 15 min |
| QW-14 | UX 중간 | `OrgRoleMappingManager.tsx:653` | 축소 가능한 섹션 토글 버튼에 `aria-expanded={autoSectionOpen}` 추가 | 3 min |
| QW-15 | UX 중간 | `TableAccessLayer.tsx:433,488` | `▶` 화살표 `<span>` 요소에 `aria-hidden="true"` 추가 | 5 min |
| QW-16 | UX 중간 | `ConflictPreviewPanel.tsx:243,253` | 장식용 `⊕` 및 `⚠️` 기호에 `aria-hidden="true"` 추가 | 5 min |
| QW-17 | UX 중간 | `RoleDefinitionManager.tsx:396` | 중복 코드 오류 `<p>`에 `role="alert"` 추가 | 3 min |
| QW-18 | UX 높음 | `TableAccessLayer.tsx:216–223` | "기본값 복원" `onResetToDefault`를 `ConfirmDialog`로 래핑 | 20 min |

**Quick Wins 소계: 18개 항목 — 예상 ~2h 40min**

---

## 전략적 항목 (높은 영향도 + 높은 공수)

| # | 출처 | 파일 | 이슈 | 예상 시간 |
|---|--------|------|-------|-----------|
| ST-A | 코드 중대 | `RowSecurityLayer.tsx:296`, `OrgRoleMappingManager.tsx:264`, `RoleDefinitionManager.tsx:208`, `ColumnMaskingLayer.tsx:474` | role/filterBase/positionLevel 캐스트 (`as DomainRole` 등) 저장 전에 런타임 멤버십 검증 추가 — 또는 `EditFormState` 타입 강화 | 3h |
| ST-B | 코드 주요 | `TableAccessLayer.tsx:467` | `useMemo`를 통해 `getOverrideCount` 결과 맵 사전 계산, `accessMatrix` 키 — 행별 렌더 비용 O(views×roles×subcats) 제거 | 1h |
| ST-C | 코드 주요 | `app/admin/page.tsx`, `AdminView.tsx` | 페이지 레벨에 `<ErrorBoundary>` 래퍼 추가, 선택적으로 `PermissionSettingsView` 내부에 탭별 경계 추가 | 2h |
| ST-D | UX 높음 | `AdminView.tsx:224` | `<main id="admin-content">` 랜드마크 + 시각적으로 숨겨진 스킵 링크 `<a href="#admin-content" className="sr-only focus:not-sr-only">` 추가 | 1.5h |
| ST-E | UX 높음 | `TableAccessLayer.tsx:420,482` | 하위 카테고리 토글 버튼에 `aria-expanded` 추가, 모듈/하위 카테고리 이름을 포함한 접근 가능한 이름을 두 토글 버튼에 추가 | 1h |
| ST-F | UX 높음 | `AccessMatrixCell.tsx:79`, `TableAccessLayer.tsx:482`, 카드 액션 버튼 | 모든 상호작용 터치 타겟을 최소 44×44 픽셀로 증가 (시각적 레이아웃 영향) | 4h |
| ST-G | UX 높음 | `ViewRowsSection.tsx:178` | 오버라이드 업그레이드/다운그레이드 시 배경색 틴트와 함께 방향 `▲`/`▼` 아이콘 (`aria-hidden="true"`) 추가 — 색상 전용 인코딩 제거 | 1.5h |
| ST-H | 코드 주요 | `RowSecurityLayer.tsx:86`, `ColumnMaskingLayer.tsx:19`, `OrgRoleMappingManager.tsx:112` | `roleLabelMap` prop 타입을 `Record<string, string>` → `Record<DomainRole, string>`로 강화 | 30 min |
| ST-I | UX 중간 | `PermissionSettingsView.tsx:310–352` | 도메인 역할 필 그룹을 `role="group"` + `aria-labelledby`로 래핑, 도메인 레이블 요소를 가리킴 | 1.5h |
| ST-J | UX 높음 | `app/admin/page.tsx:9` | Suspense fallback: `role="status" aria-label="관리자 페이지 로딩 중..."` 추가 | 20 min |
| ST-K | UX 중간 | `RowSecurityLayer.tsx:385`, `ColumnMaskingLayer.tsx:416` | 폼 다이얼로그에 `<DialogDescription>` 추가 (행 보안 규칙, 마스킹 정책) | 30 min |

**전략적 항목 소계: 11개 항목 — 예상 ~16h**

---

## 보완 항목 (낮은 영향도 + 낮은 공수)

| # | 출처 | 파일 | 이슈 | 예상 시간 |
|---|--------|------|-------|-----------|
| FI-A | 코드 주요 | `RowSecurityLayer.tsx:518`, `ColumnMaskingLayer.tsx:553`, `OrgRoleMappingManager.tsx:731`, `RoleDefinitionManager.tsx:507` | `<ConfirmDialog>` 내 IIFE `description` prop을 `useMemo` 또는 로컬 변수로 `return` 이전에 추출 | 15 min |
| FI-B | 코드 경미 | `RoleDefinitionManager.tsx:19`, `OrgRoleMappingManager.tsx:22` | 중복된 `DATA_SCOPE_OPTIONS`를 `permissionSettingsData.ts` 내 단일 공유 상수로 통합 | 10 min |
| FI-C | 코드 경미 | `TableAccessLayer.tsx:393` | 무작동 `style={{ writingMode: 'horizontal-tb' }}` 제거 | 3 min |
| FI-D | 코드 경미 | `confirm-dialog.tsx:17` | `description` prop 타입을 `string`에서 `React.ReactNode`로 확대 | 5 min |
| FI-E | 코드 주요 | `ColumnMaskingLayer.tsx:307` | 충돌 프로펜스 `Date.now().slice(-4)` ID를 `crypto.randomUUID()`로 교체, 편집 모드에서 죽은 fallback 분기 제거 | 10 min |
| FI-F | UX 낮음 | `AdminView.tsx:108` | 사용자 테이블에 `<caption className="sr-only">사용자 목록</caption>` 추가, 빈 상태 메시지를 `<TableBody>` 내로 `<TableRow><TableCell colSpan={7}>`로 이동 | 20 min |
| FI-G | UX 낮음 | `ConflictPreviewPanel.tsx:144` | `▾` 확장 화살표에 `aria-hidden="true"` 추가 | 5 min |
| FI-H | UX 낮음 | `PermissionSettingsView.tsx:205` | 헤딩 계층 수정: 섹션 헤더의 `h3` → `h2`, 하위 컴포넌트의 자식 `h4` → `h3` | 15 min |
| FI-I | UX 낮음 | `TableAccessLayer.tsx:174–190` | 감도 필터 체크박스에 명시적 `id`/`htmlFor` 페어링 추가 | 10 min |
| FI-J | UX 낮음 | `AccessMatrixCell.tsx:88` | `<PopoverContent>`에 `aria-label="접근 수준 선택"` 추가 | 3 min |
| FI-K | UX 중간 | `RowSecurityLayer.tsx:113` | SqlPreview 토글 버튼에 역할 식별 `aria-label` 추가 | 5 min |
| FI-L | UX 중간 | `AdminView.tsx:170–209` | 사용자 테이블 액션 버튼 크기를 `h-7 w-7` (28px)에서 `h-9 w-9` (36px)로 증가 | 5 min |

**보완 항목 소계: 12개 항목 — 예상 ~1h 46min**

---

## 후순위 항목 (낮은 영향도 + 높은 공수)

| # | 출처 | 파일 | 이슈 | 예상 시간 |
|---|--------|------|-------|-----------|
| DP-1 | 코드 경미 | `AdminView.tsx:125` | 동적 `style={{ backgroundColor: user.avatarColor }}`를 Tailwind 제약 아바타 컬러 클래스로 교체 | 3h |
| DP-2 | 코드 경미 | 모두 | 일관된 i18n 전략 수립 (하드코딩된 한국어 문자열) | 8h+ |
| DP-3 | 코드 경미 | `TableAccessLayer.tsx` (569L), `OrgRoleMappingManager.tsx` (746L), `RowSecurityLayer.tsx` (536L), `ColumnMaskingLayer.tsx` (568L) | 폼 다이얼로그 + 헬퍼를 형제 `*FormDialog.tsx` / `utils.ts` 파일로 추출 | 8h |
| DP-4 | UX 후순위 | `ColumnMaskingLayer.tsx:466` | 노출된 역할 체크박스 그룹을 전체 `<fieldset>/<legend>` 패턴으로 전환 | 2h |
| DP-5 | UX 후순위 | `AccessMatrixCell.tsx:88` | popover를 위한 전체 `role="menu"` / `role="menuitem"` 화살표 키 탐색 | 3h |

**후순위 항목 소계: 5개 항목 — 예상 ~24h+**

---

## Top 5 Quick Wins (권장 수정 순서)

1. **QW-1** — `'use client'` 지시자 (9개 파일) — 향후 SSR 런타임 충돌 방지 — 5 min
2. **QW-9** — 두 `TabsList` 요소에 `aria-label` — 5 min
3. **QW-18** — `ConfirmDialog`로 "기본값 복원" 래핑 — 보안/UX 안전성 — 20 min
4. **QW-13** — 결과 카운트 divs에 `aria-live` (4개 파일) — 15 min
5. **QW-8** — 편집/삭제 버튼에 동적 `aria-label` (4개 파일) — 25 min

---

## 메트릭

| 카테고리 | 수 | 예상 시간 |
|----------|-------|-----------|
| Quick Wins | 18 | ~2h 40min |
| 전략적 항목 | 11 | ~16h |
| 보완 항목 | 12 | ~1h 46min |
| 후순위 항목 | 5 | ~24h+ |
| **총합** | **46** | **~44h+** |

---

*/review-page admin을 실행하여 Quick Wins를 자동 수정합니다.*

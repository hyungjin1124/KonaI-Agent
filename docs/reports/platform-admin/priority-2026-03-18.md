# 통합 우선순위 매트릭스 — platform-admin — 2026-03-18

> 원본 리포트:
> - `reports/code-review-2026-03-18-4.md` (21개 이슈)
> - `reports/ux-review-2026-03-18-4.md` (47개 이슈)
>
> 중복 제거 후 총 **66개 고유 이슈** (양쪽 리포트에 동시 등장한 6개 항목 병합)
>
> ⚠️ v1.1 (2026-03-19): UX 리뷰 교차 검증 후 누락 9개 항목 추가 (Q-13~Q-15, S-7~S-8, F-22~F-25)

---

## 즉시 위험 — 출시 전 필수 (5개)

> 운영 환경 배포 전에 반드시 수정해야 하는 항목. 보안·데이터 손실·법적 컴플라이언스 위험.

| # | 출처 | 파일 | 이슈 | 수정 방법 | 예상 공수 |
|---|------|------|------|---------|---------|
| **I-1** | Code Critical | `PlatformAuditLogTab.tsx:77` | 빈 배열에서 `data[0]` 접근 → 런타임 크래시 | `if (data.length === 0) return;` 가드 추가 | 5분 |
| **I-2** | Code Critical | `PlatformAuditLogTab.tsx:77` | CSV 인젝션 취약점 — `=`, `+`, `-`, `@` 시작 값 무방비 | `escapeCSVValue()` 함수 추가 후 적용 | 20분 |
| **I-3** | UX Prod | `src/app/platform-admin/page.tsx` | 라우트 가드 없음 — URL 직접 접근 시 전체 테넌트 데이터 노출 | 서버 미들웨어에 `platform_admin` role 체크 추가 | 2h |
| **I-4** | UX Prod | `TenantDetailPanel.tsx:238-243` | API 키 폐기 버튼에 확인 다이얼로그 없음 — 즉시 폐기 | `ConfirmDialog` 래핑 (이유 입력 포함) | 30분 |
| **I-5** | UX Prod | `TenantDetailPanel.tsx` + `TenantManagementTab.tsx` | 운영자 행동(정지, API 키 작업)이 감사 로그에 기록 안 됨 | 핵심 액션에 audit event emit 로직 추가 | 4h |

---

## Quick Win — 즉시 적용 가능 (높은 영향 + 낮은 공수)

> 각 항목 30분 이내 수정 가능. Top 5는 ★ 표시.

| # | 출처 | 파일 | 이슈 | 수정 방법 | 예상 공수 |
|---|------|------|------|---------|---------|
| **Q-1** ★ | Code High | `TenantManagementTab.tsx:167` | `TenantDetailPanel` 재렌더 시 stale state (이전 테넌트 데이터 잔류) | `key={selectedTenant.id}` prop 추가 | 5분 |
| **Q-2** ★ | Code+UX | `PlatformAuditLogTab.tsx:108` | 기간 필터 select가 상태 미연결 — dead UI (양쪽 리포트 공통) | `periodFilter` state + `filterAuditEvents` 연결 | 1h |
| **Q-3** ★ | Code+UX | `ActivityMonitoringTab.tsx:48` | 테넌트/기간 필터 select에 `onChange` 없음 — dead UI (양쪽 리포트 공통) | `useState` + `useMemo` 필터 로직 연결 | 1h |
| **Q-4** ★ | Code+UX | `CostManagementTab.tsx:179` | 예산 기준선 `stroke="#e5e7eb"` — 배경 grid와 구분 불가 (양쪽 리포트 공통) | `stroke="#94a3b8"`으로 1줄 변경 | 5분 |
| **Q-5** ★ | Code+UX | `TenantCreateWizard.tsx:65` | 계약 종료일 < 시작일 허용 — 잘못된 계약 기간 생성 (양쪽 리포트 공통) | `isStep2Valid`에 날짜 순서 검증 1줄 추가 | 5분 |
| **Q-6** | Code High | `AlertRuleCard.tsx:19` | "편집" 버튼 onClick 없음 — 클릭해도 무반응 | `onEdit?: (rule) => void` prop 연결 | 30분 |
| **Q-7** | UX Quick | `TenantDetailPanel.tsx:96` | 상태 변경 후 성공 toast 없음 — 처리 여부 확인 불가 | `onUpdate` 콜백 후 toast 호출 | 30분 |
| **Q-8** | UX Quick | `CostManagementTab.tsx:68` | 예산 초과 경고 배너의 "알림 규칙을 확인하세요" 클릭 불가 | 버튼으로 변경 + 알림 탭 전환 트리거 추가 | 30분 |
| **Q-9** | UX Quick | `TenantManagementTab.tsx:15` | 상태 필터 레이블이 "상태"지만 실제로 healthLevel 필터 | 레이블 "헬스 상태"로 수정 | 5분 |
| **Q-10** | Code Medium | `TenantManagementTab.tsx:19` | 필터(`statusFilter`/`searchQuery`) 변경 시 `selectedTenant` 미초기화 → 패널 불일치 | 각 setter에서 `setSelectedTenant(null)` 추가 | 10분 |
| **Q-11** | Code High | `PlatformAuditLogTab.tsx:183` | 빈 필터 결과일 때 "총 0건 중 1-0" 오표시 | 빈 상태 분기 처리 | 10분 |
| **Q-12** | UX Quick | `tenantData.ts` or `TenantManagementTab.tsx` | 위험/주의 테넌트가 정렬 우선순위 없이 배치 | `filterTenants` 또는 `useMemo`에 `danger → warning → healthy` 정렬 추가 | 20분 |
| **Q-13** | UX #9 | `TenantDetailPanel.tsx` | EditableField 인라인 저장 후 시각적 확인 없음 (Q-7의 상태 변경 toast와 별개) | 저장 직후 brief green flash 또는 "저장됨" toast | 20분 |
| **Q-14** | UX P4-#6 | `ChartWidget` (전체 탭) | 모든 KPI/차트에 데이터 갱신 시점 미표시 — 운영자가 지연 데이터로 의사결정 | `ChartWidget`에 `lastUpdated?: string` prop 추가 및 표시 | 30분 |
| **Q-15** | UX P4-#11 | `src/components/ui/error-boundary.tsx` | ErrorBoundary가 platform-admin 탭 콘텐츠에 미적용 — 런타임 오류 시 전체 화면 크래시 | 각 `TabsContent`를 `<ErrorBoundary>` 로 래핑 | 20분 |

**Quick Win 소계**: 15개 / 총 예상 공수 약 5시간 30분

---

## Strategic — 높은 영향 + 중~고 공수

> 기능 완성도와 사용성을 크게 높이는 항목. 스프린트 단위 계획 필요.

| # | 출처 | 파일(들) | 이슈 | 접근 방법 | 예상 공수 |
|---|------|---------|------|---------|---------|
| **S-1** | Code High | `TenantCreateWizard.tsx`, `StatusChangeModal.tsx`, `AlertRuleEditorModal.tsx` | 모달 3종 — Escape 키·포커스 관리 부재 (WCAG 2.1 위반) | Radix UI `Dialog`로 교체 (단기: `useEffect` Escape 키 패치) | 2~4h |
| **S-2** | UX Strategic | `TenantManagementTab.tsx`, `TenantDetailPanel.tsx` | 상세 패널이 카드 목록 아래에 인라인 렌더링 — 스크롤·맥락 단절 | `fixed inset-y-0 right-0 w-[420px]` 슬라이드 패널로 교체 | 4h |
| **S-3** | UX Strategic | `TenantDetailPanel.tsx` | 복사/재발급/폐기 버튼 핸들러 전무 + API 키 발급 플로우 부재 | 핸들러 + ConfirmDialog + 신규 키 발급 모달 구현 | 6h |
| **S-4** | UX Strategic | `AlertManagementTab.tsx` | 이상 감지 규칙 편집 UI 없음 — 토글만 가능 | `sensitivity`, `baselineWindowDays` 편집 모달 구현 | 4h |
| **S-5** | UX Strategic | `TenantCreateWizard.tsx`, `TenantManagementTab.tsx:handleCreate` | 플랜 무관 고정 리소스 제한 — Enterprise 생성 시 Starter 수준 제한 적용 | 플랜별 기본값 매핑 테이블 + Step 2 미리보기 표시 | 3h |
| **S-6** | Code Medium | `TenantCreateWizard.tsx`, `StatusChangeModal.tsx` | 커스텀 토글이 `role="switch"` 미적용 — 스크린 리더 오인식 | 기존 `Switch` 컴포넌트(`src/components/ui/switch.tsx`)로 교체 | 1h |
| **S-7** | UX #18 | `TenantTableView.tsx` | 컬럼 정렬 기능 없음 — 헬스점수·에러율·토큰한도 정렬 불가 | 클릭 가능한 헤더 + `sortKey/sortDir` state 추가 | 2h |
| **S-8** | UX P3-#2 | `PlatformAuditLogTab.tsx:193`, `ActivityMonitoringTab.tsx` | 감사 로그 탭 하단 분석 차트 2개("이벤트 유형별 분포", "시간대별 볼륨")가 감사 페르소나와 이질적 | 차트 컴포넌트를 활동 모니터링 탭으로 이동 | 2h |

---

## Fill-in — 완성도 향상 (낮은 영향 + 낮은 공수)

> 개별 작업 시간이 짧고 코드 품질·사용성을 올리는 항목. 여유 시간 활용.

| # | 파일 | 이슈 | 수정 방법 |
|---|------|------|---------|
| F-1 | `TenantDetailPanel.tsx` | X 닫기 버튼 없음 | 헤더 우측에 X 버튼 추가 |
| F-2 | `PlatformAdminView.tsx:20` | 헤더 타이틀 영문/한국어 혼용 | "플랫폼 관리" 또는 영문 일관 정책 수립 |
| F-3 | `AlertHistoryItem.tsx` | severity dot 너무 작음 (w-2 h-2) | `w-2.5 h-2.5` 또는 Badge 교체 |
| F-4 | `TenantManagementTab.tsx:133` | 검색창 max-width 200px 제한 | `flex-1 min-w-[200px] max-w-[360px]` |
| F-5 | `TenantDetailPanel.tsx` | 긴 이메일/메모 overflow 처리 없음 | `truncate` + tooltip |
| F-6 | 다수 파일 | 아이콘 전용 버튼 `aria-label` 누락 (Copy, Edit2, Trash2, X 등) | `aria-label` 일괄 추가 |
| F-7 | `TenantDetailPanel.tsx:28` | `EditableField`의 `<label>` — `htmlFor` 없음 | `inputId` 생성 + `htmlFor` 연결 |
| F-8 | `costData.ts:69` | `isActual` 미사용 변수 (데드 코드) | 해당 줄 삭제 |
| F-9 | `auditLogData.ts:152`, `costData.ts`, `usageAlertData.ts` | `Math.random()` HMR 시 차트 데이터 매 저장마다 변경 | 결정론적 시드 기반 고정 데이터로 대체 |
| F-10 | `AlertRuleEditorModal.tsx:44` | 임계치 0개일 때 저장 가능 | `handleSave`에 최소 1개 임계치 검증 추가 |
| F-11 | `PlatformAuditLogTab.tsx:83` | 내보내기 파일명 고정 (`platform-audit-log.csv`) | 날짜/필터 조건 포함 파일명 |
| F-12 | `PlatformAuditLogTab.tsx` | `'⚙'` 유니코드 아이콘 사용 — 일관성 깨짐 | `Lucide Settings2` 아이콘으로 교체 |
| F-13 | `AlertManagementTab.tsx:121` | "기준선 N일" 설명 없음 | tooltip: "최근 N일 평균 대비 이상 패턴 감지" |
| F-14 | `TenantDetailPanel.tsx:212` | API 키 scope/expiresAt 미표시 | 테이블 컬럼 추가 |
| F-15 | `TenantTableView.tsx`, `AlertManagementTab.tsx` | 빈 상태 처리 없음 | "조건에 맞는 항목이 없습니다" + 필터 초기화 버튼 |
| F-16 | `TenantCreateWizard.tsx` | `grid-cols-2` 모바일 미대응 | `grid-cols-1 sm:grid-cols-2` |
| F-17 | `TenantCreateWizard.tsx` | 완료 단계를 직접 클릭하여 이동 불가 | 체크 완료 단계 클릭 이동 허용 |
| F-18 | `AlertRuleEditorModal.tsx:39` | `action` 필드 타입 단언 — 한국어 값 vs `AlertAction` 불일치 | `usageAlertData.ts`의 action 값을 `AlertAction` 타입으로 통일 |
| F-19 | `PlatformAdminView.tsx:31,38` | Banknote/CreditCard 탭 아이콘 유사 | 비용: `TrendingDown`, 과금: `Receipt`로 교체 |
| F-20 | `AlertManagementTab.tsx:11` | "전체 이력 →" 버튼 onClick 없음 | 감사 로그 탭 전환 핸들러 연결 |
| F-21 | `BillingTab.tsx:44` | "청구서 일괄 발행" onClick 없음 | 핸들러 + ConfirmDialog |
| F-22 | `TenantDetailPanel.tsx` | EditableField hover 편집 — 모바일/터치 환경에서 진입 불가 | 수정 버튼을 항상 표시(opacity-0 hover:opacity-100 → 항상 표시)하거나 "일괄 편집 모드" 버튼 추가 |
| F-23 | `SkillUsageTable.tsx` | 정렬 기능 없음 (호출 수 내림차순 등) | 컬럼 헤더 클릭 정렬 추가 |
| F-24 | `PlatformAuditLogTab.tsx` | 페이지당 6건(ITEMS_PER_PAGE) 고정 — 전문 감사 담당자에게 너무 적음 | 페이지 크기 선택 6/20/50건 드롭다운 추가 |
| F-25 | `PlatformAdminView.tsx:31,38` | "비용 관리"/"과금·빌링" 탭 이름이 사용자 멘탈 모델과 불일치 | "비용 관리" → "인프라 비용", "과금/빌링" → "테넌트 청구"로 변경 |

---

## Deprioritize — 낮은 영향 또는 현 단계 범위 외

> 현 단계에서 당장 적용 대비 효과가 낮거나 추후 API 연동 시 자연스럽게 해결되는 항목.

| # | 파일 | 이슈 | 판단 근거 |
|---|------|------|---------|
| D-1 | `TraceWaterfallPanel.tsx` | 500px 고정 너비 — 모바일 overflow | 운영 콘솔은 데스크톱 우선. 현재 우선순위 낮음 |
| D-2 | `src/app/platform-admin/page.tsx` | Suspense spinner → skeleton 레이아웃 | mock 데이터라 체감 효과 없음. API 연동 시 재검토 |
| D-3 | 전체 | 분석 이벤트(analytics) 없음 | 인프라 설계 선행 필요 |
| D-4 | 전체 테이블 | 클라이언트 전체 로드 — 확장성 한계 | API 연동 시 서버사이드 페이지네이션으로 자연 해결 |
| D-5 | `AlertRuleEditorModal.tsx` | 임계치 자유 텍스트 → percentage slider | 기능 자체 동작은 문제없음. UX 개선으로 후순위 |
| D-6 | `UsageAlertTab.tsx` | 미사용 레거시 파일 잔류 | 파일 삭제 (위험 없음, 단순 정리) |
| D-7 | 브랜드 색상 | `#534AB7` 8개 파일 하드코딩 | Tailwind 커스텀 컬러 설정은 전체 리팩토링 시 일괄 처리 |
| D-8 | `TenantManagementTab.tsx` | 테넌트 카드 페이지네이션 없음 | mock 6개. API 연동 시 서버사이드로 해결 |

---

## 기능 부재 — 신규 개발 필요

> 현재 코드에 없어 새로 개발이 필요한 기능들. 별도 스펙 작성 후 구현.

| # | 기능명 | 출처 | 페르소나 | 우선순위 |
|---|--------|------|---------|--------|
| N-1 | TenantDetailPanel 사용량 요약 서브탭 | UX S6 | 운영자, 어카운트 매니저 | High |
| N-2 | 테넌트 상태 변경 이력 히스토리 탭 | UX S1 | 플랫폼 운영자 | Medium |
| N-3 | 트라이얼 만료 임박 필터/대시보드 | UX S6 | 어카운트 매니저 | Medium |
| N-4 | 감사 로그 이벤트 상세 드로어 | UX S5 | 보안 감사 담당자 | Medium |
| N-5 | 테넌트별 비용 드릴다운 (모델별·날짜별) | UX S3 | 재무 담당자, 운영자 | Medium |
| N-6 | 알림 규칙 적용 대상 테넌트 선택 | UX S4 | 플랫폼 운영자 | Medium |
| N-7 | 미결제 테넌트 청구서 재발송 | UX S9 | 재무 담당자 | Medium |
| N-8 | CSV 일괄 테넌트 업로드 | UX S2 | 영업/어카운트 매니저 | Low |
| N-9 | 개별 청구서 PDF 생성/다운로드 | UX S9 | 재무 담당자 | Low |
| N-10 | TenantCreateWizard 닫기 시 미저장 경고 | UX S2 | 모든 페르소나 | Low |

---

## 수치 요약

| 구분 | 개수 |
|------|------|
| 원본 이슈 (code + ux) | 21 + 47 = 68 |
| 중복 병합 | 6 |
| v1.0 고유 이슈 합계 | 62 |
| 누락 항목 추가 (v1.1) | +9 |
| **최종 고유 이슈 합계** | **71** |
| 즉시 위험 | 5 |
| Quick Win | 15 (+3) |
| Strategic | 8 (+2) |
| Fill-in | 25 (+4) |
| Deprioritize | 8 |
| 기능 부재 (신규) | 10 |

**Quick Wins (I-1~I-2 포함) 총 예상 수정 시간**: 약 7시간 (보안 위험 5개 포함 시)
**Quick Wins만 (Q-1~Q-15)**: 약 5시간 30분

---

## 다음 단계 제안

```
PLAN:
Step 1 — 즉시 위험 수정 (I-1, I-2, I-4)  →  30분, 코드만 변경
Step 2 — Quick Win Top 5 (Q-1 ~ Q-5)     →  2시간 30분
Step 3 — Strategic S-6 (Switch 교체)      →  1시간
Step 4 — 나머지 Quick Win (Q-6 ~ Q-12)   →  2시간
Step 5 — Fill-in 배치 처리                →  스프린트 여유 시간
```

> **`/review-page platform-admin`** 으로 Quick Win을 자동 적용할 수 있습니다.

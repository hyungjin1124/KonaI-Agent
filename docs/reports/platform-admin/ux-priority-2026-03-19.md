# UX 우선순위 매트릭스 — 2026-03-19

## 대상
`src/components/features/platform-admin/` + `src/app/platform-admin/`

## 요약
- 총 이슈: 44건 (체크리스트: 11 / 시나리오: 22 / 구조: 4 / 프로덕션: 11)
- 프로덕션 즉시 위험: 3건
- Quick Wins: 9건 (예상 총합: 6.6시간)
- 구조 변경 제안: 4건
- 기능 부재: 12건

---

## 프로덕션 즉시 위험

| # | 관점 | 컴포넌트 | 이슈 | 출처 | 예상 공수 |
|---|------|---------|------|------|---------|
| 1 | 보안 | `useAdminGuard` | demo 모드로 항상 `setIsAllowed(true)` — 무인가 접근으로 테넌트 정지/삭제 가능 | 프로덕션 P4-1 | 4h |
| 2 | 데이터 무결성 | `TenantCreateWizard` → `TenantManagementTab` | 위저드에서 수집하는 `industry`, `adminEmail`, `useInviteLink`, `enableSso`, `deployDemoAgent` 5개 필드가 `Tenant` 타입에 없어 생성 시 전부 유실 | 시나리오 S1 / 필드 오딧 | 3h |
| 3 | 보안 | `TenantDetailPanel` | API 키 "발급", "복사", "재발급" 버튼에 onClick 핸들러 없음 — 핵심 보안 기능 부재 | 시나리오 S9 / 프로덕션 P4-2 | 8h |

---

## Quick Wins

| # | 유형 | 컴포넌트 | 이슈 | 출처 | 예상 공수 |
|---|------|---------|------|------|---------|
| 1 | 피드백 부재 | `TenantManagementTab` | 테넌트 생성 완료 시 성공 토스트 없이 위저드만 닫힘 | 시나리오 S1 | 0.5h |
| 2 | 기능 부재 | `AlertManagementTab` | 알림 규칙 삭제 기능 없음 (토글/편집만 가능) | 시나리오 S5 | 1h |
| 3 | 파생값 불투명 | `AlertRuleEditorModal` | `target` 필드가 항상 `'all'` 하드코딩, 편집 UI 미노출 | 시나리오 S5 / 필드 오딧 | 0.5h |
| 4 | 유효성 검증 | `TenantDetailPanel` (`EditableField`) | 리소스 제한 인라인 편집에 min/max/타입 검증 없음 — 음수, 0 입력 가능 | 시나리오 S8 / 프로덕션 P4-8 | 1h |
| 5 | 빈 상태 | `TenantManagementTab` | 검색/필터 결과 0건일 때 빈 상태 메시지 없음 | 체크리스트 #10 | 0.5h |
| 6 | 상태 유실 | `PlatformAdminView` | `activeTab` 및 필터 상태가 URL에 미반영 — 새로고침/공유 시 유실 | 체크리스트 #1, #31 | 1h |
| 7 | 데이터 정합성 | `TenantManagementTab` | KPI 수치 하드코딩 (`TENANT_KPI.total = "24"`) vs 실제 테넌트 6건 불일치 | 시나리오 S10 / 프로덕션 P4-3 | 1h |
| 8 | 접근성 | `StatusChangeModal`, `TenantManagementTab` | 커스텀 토글에 `role="switch"` / `aria-checked` 없음, 슬라이드오버에 `role="dialog"` 없음 | 체크리스트 #19, #21 | 1h |
| 9 | dead code | `UsageAlertTab.tsx` | `ActivityMonitoringTab` + `AlertManagementTab`에 흡수된 미사용 컴포넌트 — 어디에서도 import 안 됨 | 구조 리뷰 P3 | 0.1h |

---

## 전략적 항목

| # | 유형 | 컴포넌트 | 이슈 | 출처 | 예상 공수 |
|---|------|---------|------|------|---------|
| 1 | 구조 변경 | `PlatformAdminView` + 신규 `OverviewTab` | 6개 탭 핵심 KPI를 한 화면에 모은 "개요" 탭 부재 — 일일 점검 시 6탭 순회 필요 | 시나리오 S10 / 구조 리뷰 P3 | 12h |
| 2 | 기능 구현 | `BillingTab` | "청구서 일괄 발행" CTA 및 개별 테넌트 청구 기능 미구현 | 시나리오 S6 | 12h |
| 3 | 기능 보강 | `AgentTraceSection` | 트레이스 필터(상태/테넌트/기간) + 검색 + 페이지네이션 없음 — 장애 분석 시 전체 스캔 필요 | 시나리오 S3 | 8h |
| 4 | 탭 통합 | `CostManagementTab` + `BillingTab` | 비용($)과 빌링(KRW) 탭이 분리되어 통화 불일치 — 재무 담당자 멘탈 모델 부합 안 됨 | 시나리오 S4 / 구조 리뷰 P3 / 프로덕션 P4-10 | 6h |

---

## 구조 변경 제안

| # | 제안 | 근거 | 영향 범위 | 출처 |
|---|------|------|---------|------|
| 1 | "개요(Overview)" 탭 신설 (첫 번째 탭) | P1 운영자 일일 점검 시 6개 탭 순회 비효율 제거, 핵심 KPI + 위험 테넌트 + 최근 알림 한 화면 | `PlatformAdminView.tsx` + 신규 `OverviewTab.tsx` | 시나리오 S10 / 구조 리뷰 P3 |
| 2 | 비용 관리 + 과금/빌링 탭을 "재무 관리"로 통합, 내부 서브탭 분리 | "돈 관련" 이라는 하나의 멘탈 모델 — 원가($)와 매출(KRW)이 다른 탭에 있어 재무 담당자 혼란 | `PlatformAdminView.tsx`, `CostManagementTab.tsx`, `BillingTab.tsx` | 시나리오 S4 / 구조 리뷰 P3 |
| 3 | 에이전트 트레이스를 독립 탭으로 승격 | `ActivityMonitoringTab` 하단에 위치해 장애 분석 시 가장 중요한 기능이 가장 아래 — 스크롤 필수 | `ActivityMonitoringTab.tsx`, `AgentTraceSection.tsx`, `PlatformAdminView.tsx` | 시나리오 S3 / 구조 리뷰 P3 |
| 4 | `UsageAlertTab.tsx` 삭제 | `ActivityMonitoringTab` + `AlertManagementTab`에 흡수된 dead code | 파일 삭제만 | 구조 리뷰 P3 |

---

## 운영/품질 부채

| # | 분류 | 이슈 | 예상 영향 시점 | 출처 |
|---|------|------|-------------|------|
| 1 | 확장성 | 테넌트 카드뷰/테이블뷰 50건+ 시 전체 렌더링 성능 저하 | 고객 20개 이상 확보 시 | 프로덕션 P4-4 / 체크리스트 #25 |
| 2 | 확장성 | 트레이스 목록 전체 렌더링 — 수천 건 시 메모리 폭증 | 일일 트레이스 1000건+ 시 | 프로덕션 P4-5 |
| 3 | 확장성 | 감사 로그 클라이언트 사이드 필터링 (`filterAuditEvents`) | 로그 10만 건+ 시 | 프로덕션 P4-6 |
| 4 | 에러 처리 | 네트워크 오류/API 타임아웃 처리 없음 — 빈 화면 또는 에러 미표시 | API 연동 시점 | 프로덕션 P4-6 |
| 5 | 운영 | UI 동작(상태 변경, 리소스 수정)이 감사 로그에 자동 기록되지 않음 | 감사 대응 시 | 프로덕션 P4-9 |
| 6 | 데이터 | 인라인 편집 즉시 저장, Undo 불가 — 실수 시 복구 불가 | 운영 중 실수 발생 시 | 시나리오 S8 / 체크리스트 #12 |
| 7 | 일관성 | 날짜/시간 형식 혼재 (`2026-03-18 09:12:34`, `어제 17:08`, `3월 14일` 등) | 즉시 (지속적 혼란) | 체크리스트 #27 |
| 8 | 일관성 | 통화 표시 불일치 — 비용 탭 달러($) vs 빌링 탭 원화(KRW) | 재무 보고 시 | 시나리오 S4 / 프로덕션 P4-10 |
| 9 | 측정 | analytics 이벤트 미구현 — 기능 사용률 파악 불가 | 서비스 런칭 이후 | 프로덕션 P4-7 |
| 10 | 데이터 | 실시간 vs 집계 데이터 갱신 주기 미표시 (감사 로그 탭만 `lastUpdated` 존재) | 즉시 | 프로덕션 P4 / 체크리스트 #28 |

---

## 보완 항목
(낮은 영향도 + 낮은 공수)

| # | 이슈 | 컴포넌트 | 출처 | 예상 공수 |
|---|------|---------|------|---------|
| 1 | `AlertHistoryItem` severity dot이 색상만으로 구분 — 텍스트 레이블 없음 (색맹 미고려) | `AlertHistoryItem` | 체크리스트 #23 | 0.5h |
| 2 | `TenantHealthCard` healthScore 원형 배지에 `aria-label` 없음 — "92"만 읽힘 | `TenantHealthCard` | 체크리스트 #22 | 0.3h |
| 3 | `text-[10px] text-gray-400` 대비비 약 3.7:1 — WCAG AA(4.5:1) 미달 | 다수 컴포넌트 | 체크리스트 #13 | 1h |
| 4 | 전화번호 필드에 `type="tel"` + 포맷 마스크 미적용 | `TenantCreateWizard` | 시나리오 S1 | 0.5h |

---

## 후순위 항목
(낮은 영향도 + 높은 공수)

| # | 이슈 | 예상 공수 | 비고 |
|---|------|---------|------|
| 1 | 다크 모드 미지원 — 전체 컴포넌트 `bg-white`/`text-gray-900` 하드코딩 | 20h+ | Tailwind `dark:` 전면 적용 필요 |
| 2 | 모든 텍스트 한국어 하드코딩 — i18n 키 분리 안 됨 | 16h+ | 다국어 지원 시 필수 |
| 3 | 모든 테이블에 공통 Pagination 컴포넌트 도입 (감사 로그 제외) | 8h | 확장성 선행 과제 |
| 4 | 반응형 최적화 — 슬라이드오버 `w-[480px]`/`w-[500px]` 모바일 대응 부족 | 6h | 관리자 도구라 우선순위 낮음 |

---

## 기능 부재 목록

| # | 기능명 | 관련 시나리오 | 페르소나 | 예상 공수 | 비고 |
|---|--------|------------|---------|----------|------|
| F1 | API 키 발급 모달 (이름, 스코프, 만료일 → 키 생성 → 한 번만 표시) | S9 | P3 온보딩 담당 | 8h | 보안 핵심 — 프로덕션 즉시 위험 #3과 연관 |
| F2 | 청구서 PDF 생성 및 발행 플로우 | S6 | P2 재무 담당 | 12h | 빌링 탭 핵심 CTA |
| F3 | 플랫폼 전체 현황 개요(Overview) 탭 | S10, P3 | P1 운영자 | 12h | 일일 점검 효율화 |
| F4 | 테넌트 일괄 작업 (bulk suspend, bulk export) | S2 | P1 운영자 | 6h | 긴급 대응 시 필요 |
| F5 | 트레이스 필터/검색/페이지네이션 | S3 | P1 운영자 | 8h | 장애 분석 핵심 |
| F6 | 비용 드릴다운 (테넌트별 모델/일별 상세 브레이크다운) | S4 | P2 재무 담당 | 6h | 비용 원인 분석 |
| F7 | 테넌트 비교 기능 (복수 테넌트 나란히 비교) | P3 | P1/P2 | 10h | 벤치마크 분석 |
| F8 | 미결제 테넌트 결제 독촉 이메일 발송 | S6 | P2 재무 담당 | 4h | 미수금 관리 |
| F9 | 미저장 데이터 경고 (`beforeunload`) | P4 | P1/P3 | 2h | 위저드 중도 이탈 방지 |
| F10 | 알림 규칙 테스트 실행 (dry run) | S5 | P1 운영자 | 6h | 규칙 설정 검증 |
| F11 | 테넌트 상태 변경 이력 (who, when, from → to) | S2, S8 | P1 운영자 | 4h | 변경 추적/감사 |
| F12 | 도메인 중복 검사 | S1 | P3 온보딩 담당 | 1h | 데이터 무결성 |

---

## 반복 패턴

| 패턴 | 발생 횟수 | 관련 시나리오 | 통합 해결안 |
|------|---------|-------------|-----------|
| **유령 필드** — 입력을 수집하지만 타입에 없어 저장 후 유실 | 6회 (`industry`, `adminEmail`, `useInviteLink`, `enableSso`, `deployDemoAgent`, `webhookUrl`) | S1, S5 | `Tenant`/`AlertRule` 타입 확장 + 입력↔저장↔표시 일관성 검증 프로세스 도입 |
| **핸들러 미연결 CTA** — 버튼이 렌더링되지만 onClick 없음 | 5회 (API 키 발급/복사/재발급, 청구서 일괄 발행, 알림 전체 이력) | S6, S9 | 구현 전까지 `disabled` + 툴팁("준비 중")으로 표시하거나 제거 |
| **탭 간 데이터 단절** — 한 탭의 조치가 다른 탭에 반영되지 않음 | 4회 (테넌트 상태→감사 로그, 리소스 제한→활동 모니터링 한도, 비용→빌링 통화, 빌링→테넌트 관리) | S2, S4, S6 | 공유 상태 레이어 (Context 또는 전역 store) 도입하여 탭 간 데이터 연동 |
| **성공 피드백 누락** — 동작 완료 후 토스트/배너 없음 | 3회 (테넌트 생성, 알림 규칙 저장, API 키 발급) | S1, S5, S9 | 공통 `useToast()` 훅 + 모든 mutation 완료 시 피드백 호출 패턴 표준화 |
| **페이지네이션 부재** — 전체 데이터 한 번에 렌더링 | 4회 (테넌트 카드/테이블, 트레이스 목록, 스킬 테이블, 알림 이력) | S3, S10, P4-4, P4-5 | 공통 `<PaginatedList>` 컴포넌트 도입 + 서버사이드 페이지네이션 설계 |
